# 迁移 055：移除订单中的老师人脸门禁

在已经完成 054 的数据库中，完整执行：

1. `055-01-remove-teacher-face-order-guards.sql`
2. 检查最后返回的 3 行全部为 `READY`

本迁移只覆盖两个遗留数据库函数，不删除或修改业务数据。执行后：

- 充值、退费、正常核销和体验核销只要求老师主档及老师账号为 `ACTIVE`；
- 不再读取 `face_enrollment_status` 或 `face_person_id`；
- 核销仍强制要求客户档案照，并继续进行客户现场 1:1 人脸校验。
