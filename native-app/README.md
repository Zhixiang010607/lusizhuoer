# 原生 App（预留边界）

当前项目尚无原生 App 运行代码。未来 App 的源码、依赖、路由、平台 API、
构建配置和产物只能放在本目录，不得放入仓库根目录的网页客户端或
`miniprogram-app/`。

原生 App 应调用 `cloudfunctions/` 与 `database/` 已定义的共享服务端契约，
不得复制一套余额、权限、核销或数据库逻辑，也不得直接导入网页或小程序的
客户端实现。未来开始 App 开发时，应在本目录建立独立的依赖清单、锁文件、
环境说明与测试。
