# 露思卓儿移动业务小程序

2026-09-05 工单导出字号调整：主要数据 48→36px、标签 30→22px、标题 46→36px、说明正文 34→26px（均为 1240px 逻辑画布尺寸）；卡片高度与说明行距同步收紧。PDF、高清图片和项目样例共用同一渲染器，客户姓名＋完整编号和项目分别独占整行。字重改为标准百位值，避免部分 Canvas 将 450／750 等字重错读成字号；长说明正常分页；评价二维码保持原 220px 逻辑尺寸和白色留边，去掉卡外重复标题，短单据可与正文同页、长内容下整块换页且不裁切底部。289 项测试通过，另用隔离示例数据渲染充值、退费、正常核销、体验核销、测试评价二维码和三页长说明；用 Poppler 核对标准 A4 与视觉结果，并从 PDF 的 300 DPI 渲染图和导出长图解码回读同一测试二维码。二维码使用 example.com 排版测试地址，不关联真实工单，也未声称完成真机扫码验收。此验证使用本地 Canvas 与小程序实际渲染代码，不代表真机业务验收。本次字号修改尚未上传微信；此前 `0.2.64` 的上传超时与后台访问限制仍见[发布交接](releases/0.2.64.md)。


2026-09-05 [三类账号权限与 26 页布局检查报告](../docs/reviews/2026-09-05-miniprogram-role-layout-audit.md)：包含逐页建议、手机／平板示例截图和优先级。后续已按用户指令实施[内页布局优化](../docs/reviews/2026-09-05-miniprogram-layout-optimization.md)，检查报告截图保留为修改前证据。

这是现有 CloudBase 系统的原生微信小程序客户端，不复制 PostgreSQL 规则，直接复用测试环境 `rusizhuoer-d9gbcsgym07651694` 中的 `staffAccount` 和 `faceRecognition` 服务端逻辑。

## 当前完成范围

2026-09-05 内页优化：保留配色，统一标题、卡片和查询字号；修复创建按钮重叠、照片操作截字、审核提示遮挡、平板老师额度和工单时间省略。办理页显示实际门店与老师本人，项目／产品平板卡完整展示长名称，改密页复用当前品牌背景。三类角色范围、业务接口、单据防重、照片及 BLE 逻辑保持原规则。288 项本地回归通过，微信开发者工具使用隔离数据验证手机与 iPad 布局。

2026-09-05 老师账号登录修复：密码登录与微信手机号登录统一执行认证 UID 完整性检查、SDK 会话刷新和服务端身份回读；会话切换暂时未完成时，仅有限重试同一已认证 UID 的身份查询，不重复认证或创建账号。密码错误、封存及身份不一致继续失败关闭。创建服务、数据库绑定和云函数版本不变；详见[创建与登录检查记录](../docs/reviews/2026-09-05-teacher-create-login-check.md)。本次源码目标为 `0.2.65`，微信上传状态见[发布记录](releases/0.2.65.md)。

2026-09-05 入口视觉更新：保留现有暖象牙白、浅香槟金、深咖色值，登录页继续使用左对齐品牌、中等圆角面板、可读表单字号和全宽等高的两种登录按钮。用户反馈旧动效效果差后，已退役约 3.7 秒的背景缩放、扫光和多层淡入：现改为水滴／侧脸标志从下向上揭示，原生品牌文字晚 180 毫秒从固定视窗升起，约 1.14 秒停稳；反向裁切视窗使标志原图像素保持原位，背景、卡片和表单始终静止且完整可见。登录按钮仍只受原有会话复核与提交状态控制，减少动态效果偏好下直接显示静态终态。背景与标志继续复用 v4 图，实际 852 × 1846 像素、184,017 字节；动画终态恢复 `transform: none`，无模糊滤镜、视频、运行时依赖或 JavaScript 动画计时器。背景生成记录见 [LOGIN_BACKGROUND.md](design/LOGIN_BACKGROUND.md)。本次 289 项测试通过；官方微信开发者工具的隔离 390×844 手机预览验证了六个时间点的静止表单、标志原位揭示、播放终态、输入与密码显隐操作、按钮等宽等高及品牌不重叠，另采样 31 帧制作循环演示。Mac 锁屏使本轮平板界面切换与人工连续播放检查尚未完成，不把模拟器截图作为真机帧率或真实账号验收。预览与记录见 [登录动效重做验收](../docs/reviews/2026-09-05-login-motion-rework.md)。本次动效修改尚未上传微信。

总部、门店、老师首页统一完整文字导航、轻量标题区、卡片间距和细边框；老师基础信息／体验额度与门店联系方式适度放大，保持原模块和权限。总部筛选顺序、项目汇总、四项排名、分页与报表均保留。旧的登录夸张弧形框及三种首页必须照搬网页版像素的约定已退役。此前首页视觉验收已覆盖独立示例数据下的 390×844 手机与 768×1024 iPad、菜单定位与开关；本次仅重做登录动效，不沿用旧扫光动画的检查结果。

总部老师主页不再显示跨项目四格聚合概览，顶部改为逐个配置项目的上海自然月汇总，每张项目卡显示当前基础、本月单独充值、本月已体验和当前可用；本月充值与体验来自不可变流水和已通过体验工单，因此删除配置后重新配置也不会清空。页面继续不显示重复的独立档案卡，老师编号和联系电话直接并入顶部姓名／状态档案头；“已配置项目”卡片只保留每月基础和本月已体验两项等宽放大指标，不显示单独充值或最近更新，但下方单独充值办理表单继续保留。

1. 小程序外壳、CloudBase 连接和移动端基础 UI。
2. 现有手机号＋密码登录、用户点击授权的微信手机号快捷登录，以及登录框内与网页版共用规则的短信验证自助修改密码；验证码流程禁止自动创建陌生 Auth 用户，持久化限制 60 秒内重复发送，严格拒绝空认证结果，改密后退出临时会话并要求使用新密码重新登录。服务端只按当前 UID 回读总部／门店／老师角色。
3. 总部／门店／老师三种首页：按更新后的统一品牌规范排版移动导航、标题栏和信息卡，保留原时间筛选、四类业务汇总、横向明细表、客户表和分页。老师主页不再提前选择门店；点击建档、充值／退费、产品购买、正常核销或体验核销后直接进入完整业务主页面，办理门店选择器嵌在首个正常业务卡内，不再显示独立前置选店页。选定门店后由稳定游标自动读完该门店全部活跃客户并放入原生下拉框；姓名／生日继续作为并行精确查询入口，任填一项即可。门店账号不显示门店选择器，继续使用 UID 绑定门店。
4. 总部／门店三类数据库查询：客户查询支持门店、业务阶段、状态、建档日期、姓名／生日和直接跳页；结果按“姓名、充值、核销、门店、生日、业务阶段、建档日期、客户状态”显示并作为只读详情入口，封存／恢复只在客户主页执行。充值／核销查询支持门店、项目、类型、状态、日期、姓名／生日、直接跳页与精确工单详情，完整工单号保持单行且不截断，宽度不足由表格容器内部横向滑动承担。核销查询不再显示不适用于正常／体验核销的“待审核／已通过／已驳回”汇总卡，只在结果卡显示总条数；充值／退费仍保留审核汇总。门店查询由服务端锁定本店，总部可以选择全部或具体门店；老师不进入通用客户查询页，只从本人工作台的业务明细和有权限的客户列表进入客户主页。
5. 客户主页、客户历史工单链接和数据库工单详情；客户主页只展示并预览客户照片，不提供重读或相册保存，项目／产品和业务老师只显示名称／姓名，留言发送按钮在输入框下方完整居中。业务记录固定拆成充值、退费、核销、体验、产品五个独立标签，“产品”只显示产品购买工单并以件为数量单位，五类由服务端分别筛选并各自进行稳定游标分页；每个标签每页固定 20 条，以“上一页／当前页／下一页”替换当前结果，不再累加成“已加载”列表。核销详情只展示本次核销照片和三张补充照片，最初建档照片不进入工单；每张已上传照片支持查看原图、保存相册和转发，原图优先复用 24 小时本地缓存，本地文件不存在或过期时才重新请求该照片，并对并发操作合并为一次下载。工单底部显示与当前正常／体验核销唯一绑定的客户评价：未提交时只显示“暂无评价”，提交后显示门店环境、老师服务、整体体验 1—5 星和客户留言。未评价时总部或门店已导出的同一二维码不设有效期并永久有效；提交成功后评价永久锁定，原二维码只能查看结果，不能修改或再次提交。原提交账号仍可在服务端时限内补充照片。现有充值、退费、核销和体验工单均使用与项目样例相同的单据排版导出 PDF／图片，PDF 按标准 A4 真正分页；全部可导出工单和四种项目样例统一采用字段标题小、真实数据大的可读层级，详情最多两列，完整时间优先整行，任何长值都不能缩成迷你字。总部和工单所属门店导出的正常／体验核销凭证都可追加评价二维码，只有老师导出保持原版无二维码、无照片、无留言版式。充值／退费凭证前两行依次为“客户姓名＋完整客户编号／项目”和“门店／业务老师”，充值信息只保留次数、提交时间与审核时间，业务前后余额不显示。赠品存在时单独开板块且只打印名称和数量，没有赠品时不显示。项目四种样例、真实长图和 PDF 每页统一使用无内嵌文字的露思卓儿暖象牙白／浅香槟金品牌背景，正文保持深咖色；当前客户端按每页 `2480 × 3508` 像素（300 DPI）逐页渲染，属于高清位图 PDF／图片，不冒充无限放大的真矢量文件。
6. 客户现场建立、充值申请、退费申请；充值与退费提交成功或防重恢复到完整原单据后，直接进入对应工单详情，详情按同一工单 ID 与请求编号回读成功后才清除防重锁。
7. 正常核销和老师体验核销；两者都是已选客户的 1:1 人脸校验，并且都要求办理人员在选定项目后自行填写 `1—999` 次，不能固定为 1。输入上限不超过当前显示余额／额度；提交指纹、数据库记录、体验额度流水和设备信号回读必须使用同一次数。核销成功或防重恢复完成后直接进入对应核销工单详情，同样只在详情回读确认后清除防重锁。
8. 总部管理：门店按名称／联系人电话、老师按姓名／联系电话查询，查询结果与活跃／封存分区均使用网页版手机端同款横向表格；老师目录只保留姓名、编号、联系电话和状态四列，新增进入独立页面。门店主页包含基础资料、时间范围、项目汇总、四类有效业务明细、精确工单／客户跳转、分页、活跃／封存客户和数据库回读确认的状态操作；老师主页包含档案、临时密码重置、账号状态、体验总览、项目累计、配置／充值／删除额度和完整历史。项目管理使用独立“全部项目 → 项目创建 → 项目单据模板”流程，模板支持共用原始 LOGO、两组共用说明、保存后数据库复核、项目封存／激活和核销／充值 PDF 与图片四种真实样例预览及导出；LOGO 区不显示文件名、编号、大小、像素或压缩说明。与项目并列的产品管理同样使用“产品列表 → 独立产品创建”流程：列表页不放输入框，点击“新增产品”后在独立页面只填写产品名称，成功后返回列表并显示数据库自动编号；产品只能封存或重新激活，没有类别、介绍、模板、配置和删除入口。
9. 总部充值／退费与产品购买审核：总部“审”入口不再出现没有现行流程的核销审核；充值审核页可切换退费审核，产品购买使用独立审核列表和简洁工单详情。条件／精确工单查询、门店与状态筛选、100 条分页、直接跳页、真实提交／审核时间、工单／客户详情和可选审核留言均保留。

正常核销与体验核销的人脸通过后现在进入 BLE 设备授权阶段：服务端先建立 90 秒资格，小程序可在资格有效期内关闭并重新打开扫码窗口；扫码并连接设备后转发最长 30 秒的一次性 HMAC 授权。只有同一设备回报工作状态 `2`，服务端才调用既有数据库原子写入扣减次数并生成核销工单；成功后扫码窗口永久关闭并跳转该工单。扫码取消、蓝牙关闭、权限拒绝、设备／项目不匹配、连接或协议失败、设备未进入工作态均不得扣次。协议与部署说明见 `database/cloudbase-console/066-README.md` 和独立 BLE 技术说明 PDF。

> 当前仓库代码按 `staffAccount v81`、`faceRecognition v113`、`verificationPhoto v11`、`teacherCreate v6`、`customerRating v7` 编排，当前已上传的小程序开发版本为 `0.2.61`。补充照片先取得与数据库任务绑定的一次性 HTTPS PUT 地址，再用 `wx.request` 直接发送 JPEG `ArrayBuffer`；提交云函数不再携带 Base64 图片或上传证明。员工函数仍须在服务端按 UID 和人员主档鉴权，数据库不得向匿名角色开放。代码推送、SQL、云函数、静态网页、小程序开发版上传、设为体验版、提交审核和正式发布仍是独立状态。

> 2026-09-05 最新发布状态：包含登录、三类首页与内页优化的 `0.2.64` 已准备并推送，288 项测试通过；官方 CLI 上传返回 `code 10 / Timeout`，开发者工具界面重试约五分钟后也明确显示“上传失败 / Error: Timeout”，两次均未取得成功回执。当前最后一个确认上传成功的开发版本仍为 `0.2.61`。本次上传前工具提示“上次提交已被选为体验版，本次上传将会覆盖体验版”，因此旧记录中“尚待设为体验版”不再代表当前事实，但这不能证明 `0.2.64` 已更新到体验版。浏览器工具的站点安全策略再次明确禁止自动化访问公众平台；该拦截没有触发用户许可或自动审批，未绕过。本次未提交审核或正式发布，没有新 SQL、云函数或静态网页部署。 详见[0.2.64 发布交接](releases/0.2.64.md)。

> 2026-09-05 已部署：活跃预警、余次预警的两类首屏及完整导出改为一次服务端数据库快照；评价分析的汇总、图表、列表和完整导出也不再跨请求拼接。评价提交遇到 CloudBase 空 `RETURNING` 时由 `customerRating v7` 有界重读确认。运营指标 PDF／Excel 的旧 10000 条上限已退役，统一改为压力数据验证后的 1000 条，并让长查询条件自动换行；PDF 随三个导出分包内置 Brotli 压缩的 OFL 简体中文子集字体，不再依赖阅读器额外安装 Adobe-GB1 语言包。`faceRecognition v113` 与 `customerRating v7` 已由官方 CloudBase CLI 成功上传，状态均为 `Active`，线上回下载文件与仓库 SHA-256 全部一致；控制台测试编辑器加载失败，所以本轮运行时 `health` 仍明确待补。小程序 `0.2.61` 已由官方 CLI 上传为开发版本，主包 `1,607,496` 字节、总包 `4,360,674` 字节，三个运营指标分包分别为 `791,616`、`793,491`、`797,773` 字节。旧 iPad 不再依赖 `conic-gradient` 绘制圆环，小程序每次启动和回到前台都会请求屏幕常亮。公众平台禁止自动化访问，`0.2.61` 尚待管理员手动设为体验版；没有新 SQL。

> 2026-09-05：`faceRecognition v112` 已通过官方 CloudBase CLI 部署，平台回执 3 个文件／84.0 KB、`success=true`，状态为 `Active`；小程序真实调用 `health` 返回 `ok=true`、`version=v112`，私有照片存储检查正常。`0.2.60` 已通过微信开发者工具官方 CLI 上传为开发版本，主包 `1,603,632` 字节、总包 `2,064,820` 字节。它把活跃预警按客户拆成“全部项目为 0／任意项目非 0”，把余次预警按客户×项目拆成“项目余次为 0／非 0 且低于阈值”；四个结果区只统计未封存客户，各自每页 20 条并独立翻页。“营”栏中的活跃预警、余次预警和评价分析均可导出当前完整筛选结果的矢量 PDF 与 Excel，按门店分组，单次上限统一为 10000 条，超过时拒绝导出并要求缩小条件。已登录总部会话对两个生产查询完成空结果只读复验；当前没有命中记录，未建立测试客户。本轮不改数据库结构、没有新 SQL；公众平台页面不能由自动化进入，`0.2.60` 仍待管理员手动设为体验版，未提交审核或正式发布。

> 2026-09-03：`0.2.59` 已通过已登录微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，主包 `1,594,721` 字节、总包 `2,033,657` 字节。评价分析的“评分／状态、数量、占比”使用与其他查询结果相同的全宽内容自适应表格，手机数据字号为 `40rpx`、iPad 为 `22px`，数量和百分比保持独立列且列宽按真实内容计算。两张圆环改为普通页面内绘制，不再使用会在重排后漂移的原生 Canvas；iPhone 15 Pro Max 与 768×1024 iPad 模拟器实测圆环正常显示，手机上下单列、iPad 左右双列，三列表格与卡片均无重合、空白或越界。279 项自动化全部通过。本轮不改云函数、数据库或静态网页、没有新 SQL；`0.2.59` 尚未设为体验版、提交审核或正式发布。

> 2026-09-03：`0.2.58` 已通过已登录微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，主包 `1,594,721` 字节、总包 `2,033,118` 字节。评价分析保留 0—5 分任意多选查询，但评分分布只统计已评价工单的 1—5 分并以已评价总数计算比例；0 分未评价只进入评价覆盖率图。两张饼图及中心数字、图例、比例和说明文字均已放大。评分按钮在手机固定两行三列、iPad 固定一行六列，并用明确宽高消除原生按钮溢出；iPhone 12/13 Pro 与 iPad Pro 10.5-inch 开发者工具预览均无重合、越界或图例挤压。279 项自动化全部通过。本轮不改云函数、数据库或静态网页、没有新 SQL；`0.2.58` 尚未设为体验版、提交审核或正式发布。

> 2026-09-03：`0.2.57` 已通过已登录微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，主包 `1,594,721` 字节、总包 `2,032,892` 字节。全部可导出的充值、退费、正常核销、体验核销工单和项目四种样例同步使用放大后的共同渲染器：字段标题 30、数据值 48，单行长值最低 34，详情最多两列且完整时间整行显示；PDF 与图片采用同一层级。对应网页版 `0.15.26` 已安全发布并逐文件核对远端 SHA-256。扫码评价完成页不再出现面向老师、门店和总部的查看说明。本轮不修改云函数或数据库、不需要新 SQL；`0.2.57` 尚未设为体验版、提交审核或正式发布。

> 2026-09-03：`0.2.56` 已通过已登录微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，主包 `1,594,665` 字节、总包 `2,032,836` 字节。它把工单补充照片从 Base64 云函数中转改为签名直传：小程序仍先在本地校验格式并把非 JPEG 或过大图片压到不超过 3 MB，然后仅把原始 JPEG `ArrayBuffer` PUT 到服务端签发的随机私有对象；上传完成后，`verificationPhoto v11` 回读真实对象并原子绑定工单。签名或上传失败会取消精确任务，不会删除已绑定照片。`faceRecognition v111` 与 `verificationPhoto v11` 已部署并通过线上健康检查；上海区内 145 KB、12 路并发和接近 3 MB 的并发上传／下载均全量成功且哈希一致。本次不需要新 SQL；`0.2.56` 尚未设为体验版、提交审核或正式发布。

> 2026-09-03：`0.2.55` 已通过已登录微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，主包 `1,594,665` 字节、总包 `2,031,887` 字节。运营核心指标查询新增“评价分析”：总部可选全部／指定门店，门店锁定本店；两者可按项目、业务老师（含未指定）、上海业务时间范围和 0—5 分任意组合查询。0 分表示未评价，已评价工单取三项评分最低分，无业务老师时取其余两项最低分；结果每页 20 条并可进入工单／客户详情。该版本当时将未评价放进 0—5 分分布，此显示口径已由 `0.2.58` 退役；当前评分分布仅包含 1—5 分，未评价只进入覆盖率图。未被代码引用的本地 `services/Untitled.png` 保持原文件不变并由上传配置排除。本轮不修改数据库、没有执行新 SQL；`0.2.55` 尚未设为体验版、提交审核或正式发布。

> 2026-09-02 当前 `0.2.54` 待上传目标：总部与门店新增独立“运营核心指标查询”下拉栏，内含“活跃预警”和“余次预警”，不再混放在普通“查”菜单。“活跃预警”按正常核销 → 体验核销 → 客户建档的固定优先级和手填天数计算，结果固定为客户／门店／间隔时间／计算起点／上次核销五列并把间隔最长者排在最前；“余次预警”只统计已经开卡的权威项目余额，支持单个／全部活跃项目和手填严格小于阈值，结果只显示姓名、项目名称、当前剩余、净开卡、已核销和门店，不在项目名称后追加编号，也不显示生日。两页均由已上传且平台状态为 Active 的 `faceRecognition v110` 在服务端排除封存客户、锁定总部全部／指定门店或门店本店范围，并提供每页 20 条、直接跳页和客户主页入口；页面还可以把当前条件下全部结果一次性导出为按门店分组的 Excel，最多 10000 条且不会静默截断。函数 `health` 尚待已登录控制台会话复验。该小程序代码目标不代表已经上传为开发版。

> 2026-09-02：`0.2.51` 已通过已登录的微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，CLI 返回 `✔ upload`，总包 `1,948,225` 字节。总部老师主页顶部现在按每个配置项目显示上海自然月的当前基础、本月单独充值、本月已体验和当前可用；自然月数据来自不可变流水与已通过体验工单，删除后重新配置不会清空历史。`staffAccount v81` 与 `customerRating v3` 当时已部署并通过线上健康检查；后续总部真实导出证明 v3 仍会把 CloudBase 已提交但空 `RETURNING` 的令牌更新误报为失败，因此该签发可靠性结论已由 v4 取代。网页版 `0.15.24` 的 5 个文件也已发布并逐文件核对远端哈希。该记录只确认开发版本上传成功；`0.2.51` 尚未由管理员设为体验版、提交审核或正式发布。

> 2026-09-02：`0.2.50` 已通过已登录的微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，CLI 最终返回 `✔ upload`，总包 `1,946,340` 字节。`customerRating v2` 已部署且线上健康检查返回 v2，网页版变更文件也已发布并逐文件确认远端与本地一致。该版本允许总部和工单所属门店导出的正常／体验核销凭证追加评价二维码，只有老师导出保持无二维码原版；未评价二维码永久有效，提交后不可修改或再次评价。该记录只确认开发版本上传成功；`0.2.50` 尚未由管理员设为体验版、提交审核或正式发布。

> 2026-09-01：`0.2.49` 已通过已登录的微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，CLI 最终返回 `✔ upload`，总包 `1,946,323` 字节。该版本新增门店核销凭证评价二维码，以及工单底部“暂无评价”或三项星级和客户留言显示；老师和总部导出的凭证继续不带二维码。迁移 068、`customerRating v1`、匿名登录、OPA 函数网关权限、函数安全规则和静态 `rating.html` 已同步部署，匿名评价边界线上回归通过。这里记录的是当时版本；其中“总部不带二维码”规则已于 2026-09-02 被 `customerRating v2`／小程序 `0.2.50` 明确退役。`0.2.49` 未由管理员设为体验版、提交审核或正式发布。

> 2026-08-29：`0.2.48` 已通过已登录的微信开发者工具官方 CLI 上传到 AppID `wxb053c1bd6c684d8b` 的开发版本，CLI 最终返回 `✔ upload`，总包约 `1.8 MB`。该版本为查看原图、保存相册和转发共用 24 小时持久本地缓存；同一照片的并发操作合并为一次请求，本地文件缺失、过期或照片身份变化时才重新获取。核销／体验工单详情只显示本次核销现场照和三张补充照片，最初建档照片不显示；核销 PDF／图片采用与充值单一致的无照片、无留言版式。该记录只确认开发版上传成功；公众平台因页面安全策略不能由自动化代点，`0.2.48` 尚待管理员在“管理 → 版本管理 → 开发版本”手动选择为体验版，未提交审核或正式发布。

> 2026-08-28：`0.2.41` 已于 22:29 AEST 通过已登录的微信开发者工具上传为开发版，总包 `1,878,902` 字节。总部与门店的客户、充值、退费、核销和产品查询首次进入统一默认上海业务日“今天”，点击重置也回到今天；“全部时间”仍可在时间选择器中手动选择。该记录仅代表开发版上传成功，尚未设为体验版、提交审核或正式发布。

> 2026-08-29：`0.2.46` 已通过已登录的微信开发者工具 CLI 上传为开发版，总包 `1,925,995` 字节。客户主页抬头及项目汇总的累计充值只统计审核通过的 `NEW` 历史充值，审核通过的 `REFUND` 作为累计退费单列；剩余次数仍读取数据库净余额。所有凭证不打印留言。核销工单详情只展示本次核销照片和三张补充照片，最初建档照片不显示；核销凭证只保留本次核销照片。总部主页原有充值／退费分列口径保持不变，并由合同测试防止后续误改。该记录仅代表开发版上传成功，尚未手动设为体验版、提交审核或正式发布。

> 2026-08-29：`0.2.47` 已通过已登录的微信开发者工具 CLI 上传为开发版，总包约 `1.8 MB`。该版本修复 BLE 资格写入后空返回导致的 `verification_id` 异常，资格改为按幂等键回查；缺少或过短的 `BLE_AUTH_SIGNING_KEY` 会在资格创建前受控拒绝，前端只显示简短中文反馈。核销页说明和按钮文案已精简，资格按钮随页面正常滚动、不再悬浮。该记录仅代表开发版上传成功，尚未手动设为体验版、提交审核或正式发布。

> 2026-08-28：`0.2.40` 已于 22:16 AEST 通过已登录的微信开发者工具上传为开发版，总包 `1,878,736` 字节。门店资料的地区和详细地址改为各占整行并完整换行，长地址不再显示省略号；联系人和联系电话继续保持紧凑两列。该记录仅代表开发版上传成功，尚未设为体验版、提交审核或正式发布。

> 2026-08-28：`0.2.39` 已于 22:00 AEST 通过已登录的微信开发者工具上传为开发版，总包 `1,878,361` 字节。门店四类可办理业务都允许不指定业务老师；主动选择老师才校验并产生老师归属，老师账号仍只绑定本人。该记录仅代表开发版上传成功，尚未设为体验版、提交审核或正式发布。

> 2026-08-28：`0.2.37` 已通过已登录的微信开发者工具 CLI 上传为开发版，代码包约 1.8 MB。补充照片允许选择单张不超过 7 MB 的 JPG／JPEG、PNG 或 WebP，本地校验真实文件头后统一按需转换为不超过 3 MB 的 JPEG，再进入保持私有、桶级上限 7 MB 且只接收 JPEG 的 `customer-photos` 存储桶；固定两张人脸证据和服务端权限／时限校验不变。总部充值／退费与产品购买审核共用紧凑筛选比例，字段和按钮同高对齐；结果表按内容分配列宽，工单号、门店和时间保留独立留白；空结果不显示无意义的上一页、下一页和跳页区。上传、体验版、审核和正式发布状态仍分别计算。

> 2026-08-28：`0.2.35` 已通过已登录的微信开发者工具 CLI 上传为开发版，代码包约 1.8 MB。该版本进一步压缩平板客户查询和总部审核工作台，统一控件高度、字号、底边对齐和五行结果视口，并收紧审核表格安全列宽。总部完整排名的门店与老师都只显示名称／姓名，编号只保留为内部定位数据；项目汇总继续按数据量自适应高度。上传不等于体验版、审核或正式发布，后三项尚未执行。

> 2026-08-28：`0.2.30` 已上传为开发版。该版本新增独立的 `>= 700px` 平板布局断点，不再把手机 `rpx` 页面按 iPad 宽度整体放大；主体内容限宽，导航、字号、卡片、输入框、按钮、分段选择、表格与分页改用封顶像素尺寸，并为按钮和选项保留固定间距。门店资料在平板上只对合适的信息卡分栏，项目汇总按内容自适应高度，业务明细和活跃／封存用户按 `0—5` 条适配高度，超过 5 条继续在卡片内部滑动；封存用户卡恢复与其他页面一致的暖象牙底色，仅状态徽标保留语义色。手机布局和业务逻辑不变。尚未手动设为体验版、提交审核或正式发布。

> 2026-08-28：`0.2.29` 已上传为开发版。该版本在产品查询增加“全部来源／产品购买／充值赠送”，显示明确来源并让充值赠送进入父充值单；产品购买审核和详情仍强制只读 `PURCHASE`。微信小程序身份源已关闭透明模式和 UID 复用并开启同手机号自动关联；小程序仍在每次登录前退出旧 SDK 会话，微信手机号授权后核对平台 UID，绝不按客户端手机号自行改绑。配套 `staffAccount v79` ZIP 上传并通过 `health` 前，统一产品查询不能视为线上生效。尚未手动设为体验版、提交审核或正式发布。

## 登录最终规则

- 手机号＋密码与微信手机号授权必须登录到同一既有 CloudBase Auth 用户和同一 UID；微信授权只是该账号的另一种认证方式，不创建第二业务身份。
- 每次用户主动发起手机号密码或微信手机号登录前，客户端先退出 SDK 中可能残留的旧 CloudBase Auth 身份，再使用本次认证结果调用 `staffAccount.session`；不得让上一次账号令牌进入本次角色回读。
- 手机号授权只能由用户点击 `open-type="getPhoneNumber"` 按钮发起。小程序只把按钮返回的一次性 code 交给 CloudBase SDK，不解密、不记录、不持久化该 code。
- CloudBase 验证微信手机号并关联既有 Auth 用户后，小程序调用无手机号参数的 `staffAccount.session`。服务端只按平台认证的当前 UID 回读员工、角色、账号状态和门店范围。
- 客户端输入或传入的手机号不是身份证明。禁止用手机号自动创建或替换 `staff_accounts.auth_uid`；UID 未命中已有员工账号时必须登录失败、退出 CloudBase 会话并清理本地业务会话。
- 陌生手机号不得自动注册 CloudBase Auth 用户、员工账号或业务角色。封存员工即使仍有未过期客户端令牌，每次服务端回读也必须拒绝。
- 手机号＋密码继续作为应急和配置故障时的备用登录方式。
- App 启动时本地缓存会话只用于发起服务端复核；`restoreAndValidateSession` 完成前，登录按钮、微信手机号按钮、修改密码入口和深链页面全部 fail-closed。复核完成后只使用本轮写入的 `globalData.session`，失效缓存不能短暂进入任何角色主页。

## CloudBase 与微信前置配置

1. 在与现有 Auth 用户、云函数和 PostgreSQL 相同的 CloudBase 环境中启用 `WX_MICRO_APP` 身份源，配置当前小程序 AppID 和 AppSecret。
2. 显式配置 `On=TRUE`、`AutoSignInWhenPhoneNumberMatch=TRUE`、`TransparentMode=FALSE`、`ReuseUserId=FALSE`，并保持用户名／手机号＋密码登录开启。全托管持久身份模式可能固定回显 `AutoSignUpWithProviderUser=TRUE`；这不得创建或改绑任何业务账号，未映射 UID 必须登录失败。不得依赖控制台默认值。
3. 将 `staffAccount`、`faceRecognition`、`verificationPhoto` 等业务云函数的安全规则继续限定为已登录且非匿名用户；微信登录成功不代表已获得总部、门店或老师权限。核销照片清单和高清原图由独立 `verificationPhoto` 服务读取，不把照片流量重新压回人脸与业务主函数。
4. AppSecret 只能保存在 CloudBase 身份源的服务端密钥位。快捷登录不新增小程序端密钥，不需要自建微信解密云函数、Custom Login 私钥或自定义 ticket。
5. 微信小程序必须是已完成认证的非个人主体。发布前在微信公众平台完成手机号用途的用户隐私保护指引、接口声明和页面明确授权文案。
6. 按微信和 CloudBase 当期政策开通小程序手机号验证能力及相应套餐／计费；模拟器无法替代真实微信账号和真机验收。

## 已验证开发基线

- 当前开发 AppID：`wxb053c1bd6c684d8b`。AppID 是公开标识，不是密钥；若以后切换正式小程序，必须同时更新 `project.config.json`、微信公众平台合法域名和 CloudBase 环境授权。
- CloudBase 测试环境：`rusizhuoer-d9gbcsgym07651694`，区域 `ap-shanghai`。
- CloudBase JS SDK：`3.7.1`，与当前网页版登录 SDK 对齐；`@cloudbase/adapter-wx_mp` 为 `1.3.1`。
- Node.js 最低版本：`20.19.0`；当前已验证版本为 Node.js `24.19.0` LTS。
- 包管理器：pnpm `9.15.9`，版本已写入 `miniprogram/package.json`。仓库只维护 `pnpm-lock.yaml`，不要另行生成 `package-lock.json`。
- 微信开发者工具已验证版本：macOS Apple Silicon `2.02.2608040`。
- 仓库目标服务端版本：`staffAccount v81`、`faceRecognition v113`、`verificationPhoto v11`、`teacherCreate v6`、`customerRating v7`。
- 2026-08-26 最新上传的开发版 `0.2.18` 总包为 `1,761,825` 字节，主包为 `1,502,103` 字节，均低于微信限制。它继承 `0.2.17` 的项目／产品隔离、独立产品创建、全量客户下拉、完整总部汇总／排名、默认今天和充值第三步产品赠予；赠品加号使用受容器约束的弹性布局和独立符号层，不会再被推出卡片右侧。充值详情和导出仅在确有赠品时显示产品名称、编号与数量；客户确认按钮及产品列表封存／激活按钮均强制水平和垂直居中，产品状态按钮不再撑满操作列。保存新临时密码、老师状态管理和单独充值体验次数办理仍完整保留。启动页和共用 WXSS 页面留在主包，其余 14 个业务页使用原目录普通分包；分包不得改成 `independent`，否则会失去主包的会话、服务、组件和全局样式。上传配置会过滤不可达 npm 产物、sourcemap、类型声明和本地依赖元数据，测试会保留至少 200KB 的主包预算余量。
- 小程序登录页继续使用露思卓儿暖象牙白／浅香槟金品牌背景及原生文字品牌名，2026-09-05 起采用左对齐品牌、中等圆角暖白表单、清晰字号和全宽等高的登录按钮；旧的夸张弧形框与居中品牌排版已退役。微信手机号登录、眼睛图标、密码下方右侧“修改密码”和全部认证逻辑保留。三类首页使用完整文字顶部导航、轻量标题和统一暖象牙卡片；业务模块顺序不变，小程序与网页版保持物理隔离。

## 首次安装、导入与运行

1. 安装 Node.js `>=20.19.0`，并启用仓库指定的 pnpm：

   ```bash
   corepack enable pnpm
   corepack install --global pnpm@9.15.9
   ```

2. 从仓库根目录安装小程序依赖：

   ```bash
   cd miniprogram-app/miniprogram
   pnpm install --frozen-lockfile
   ```

   `--frozen-lockfile` 防止依赖漂移；版本控制内的 `miniprogram/.npmrc` 固定使用 `node-linker=hoisted`，生成微信开发者工具更容易识别的扁平依赖布局。

3. 在微信开发者工具中导入仓库内的 `miniprogram-app` 目录，不要误选下一层 `miniprogram`。
   现有 CloudBase 是 PostgreSQL 环境；如果开发者工具提示“不支持 PostgreSQL 环境”，直接取消“转换云环境”。不要新建第二环境，小程序会通过 CloudBase JS SDK 和微信适配器调用同一环境中的现有云函数。
4. 选择“工具 → 构建 npm”，确认生成 `miniprogram/miniprogram_npm`。macOS 开启“设置 → 安全设置 → 服务端口”后，也可从仓库根目录执行：

   ```bash
   /Applications/wechatwebdevtools.app/Contents/MacOS/cli build-npm \
     --project "$(pwd)/miniprogram-app" \
     --compile-type miniprogram
   ```

5. 在 CloudBase 中保持现有“用户名/手机号＋密码”登录方式可用，并把 `https://rusizhuoer-d9gbcsgym07651694.ap-shanghai.tcb-api.tencentcloudapi.com` 配入当前 AppID 的微信公众平台 `request` 合法域名。本机开发可以在被 Git 忽略的 `project.private.config.json` 中临时关闭域名校验，但预览、真机和发布不得依赖该开关。2026-08-25 检查时 CloudBase 服务商域名入口提示本月 50 次修改额度已用完；不要继续重复提交，可改为在微信公众平台直接配置，或等待额度恢复。
6. 按“CloudBase 与微信前置配置”启用 `WX_MICRO_APP`，并完成微信主体、认证、隐私和计费前置。CloudBase 环境授权成功不等于微信手机号快捷登录已经启用；合法域名、身份源和真机验收缺一不可。
7. 分别验收密码和微信授权登录。服务端 `session` 回读的 UID、角色和门店必须与既有员工身份一致，两种方式的 UID 必须完全相同，否则不进入工作台。

当前 SDK 依赖在微信开发者工具构建 npm 时可能提示 `bson/lib/bson.cjs.js: Npm package entry file not found`。在上述已验证版本中，npm 构建、登录页编译和模拟器启动均成功，控制台没有运行错误；如果以后升级 SDK 后出现实际的 `module not found`，应统一升级或修正 CloudBase SDK 依赖，不能手工复制一个伪造的 `bson.cjs.js` 掩盖问题。

拍照／相册组件使用 `wx.chooseMedia`，但它不是 `app.json.requiredPrivateInfos` 允许声明的定位类接口，不能把 `chooseMedia` 加回该数组。发布前应在微信公众平台按实际采集用途完成客户照片和员工手机号的用户隐私保护指引、对应接口声明与明确授权说明。

`miniprogram/config/env.js` 只包含公开的环境 ID、区域和函数名。人脸 SecretId、SecretKey、CloudBase API Key、PostgreSQL 密码与 SQL 不得放进小程序。

## 本地自动验收

从仓库根目录执行：

```bash
node --test tests/*.test.js
git ls-files -z '*.js' | xargs -0 -n 1 node --check
cd miniprogram-app/miniprogram
pnpm list --depth 0
pnpm audit --prod
```

交付前还必须在微信开发者工具重新构建 npm、编译登录页与本轮修改页面，并检查模拟器控制台没有运行错误。总部只读验收应真实打开总部首页、项目／产品／门店／老师管理、充值／退费审核和产品购买审核；核销审核已经退休，不再作为验收入口。`node_modules`、`miniprogram_npm` 和本机 `project.private.config.json` 均已被 Git 忽略。

## P0 防重要求

充值／退费和核销在调用前先把不含客户明文、留言和人脸令牌的请求指纹与 `clientRequestId` 写入小程序本地存储。请求超时或断网后：

- 不会生成新编号再次提交；
- 只能用原 `clientRequestId` 调用 `recoverBusinessSubmission` 查库；
- 数据库结果不确定时继续锁定；
- 只有查到原工单完整成功，或服务端确定拒绝且查库为空，才解除锁定。

此逻辑依赖线上数据库已完整执行相应迁移，尤其是防重字段、核销照片、体验额度审计、设备信号与恢复查询。

## 发布前手工验收

- 同一员工分别使用密码和微信手机号授权登录，CloudBase UID 必须完全相同，角色、状态和门店范围必须一致。
- 用户拒绝授权或授权事件没有 code 时不发起登录；一次性 code 不复用、不记录、不写入本地存储。
- 授权手机号未绑定员工时，CloudBase 和 PostgreSQL 都不新增业务账号，且客户端退出 CloudBase 会话；不能通过伪造手机号修改 `auth_uid`。
- 封存员工的密码与微信授权登录均被服务端拒绝；退出后更换微信用户不能继承上一个员工的本地会话或老师所选门店。
- 分别用总部、门店、老师账号登录；总部看到查询、管理和审核但没有一线办理，门店无体验核销。
- 总部首页依次检查“总览／查询／运营／管理／审核／退出”：首次进入和重置均为“今天＋门店＋全部项目＋充值”；时间周期、排名对象、项目范围、排序指标按顺序放在同一筛选卡中。非自定义周期不显示日期框，自定义周期才展开起止日期。筛选卡后直接显示全部项目汇总，每页10项并显示核销、充值、体验、退费，底部合计必须是当前日期范围的全局合计；页面不再显示六张独立指标卡或重复的“分类统计／前 10 名”。项目范围保留有效与已封存项目，封存项带明确标识。排名对象只选门店／老师，项目范围选全部／具体项目，排序指标选充值／核销／体验／退费，三项组合由服务端精确排序；当前激活主档即使本期 0 次也参与，已封存主档只在本期确有业务时参与。完整排名使用不越界卡片。上一页／下一页、页码跳转、单独重试和包含全部项目汇总与完整排名的矢量 PDF 导出均可用，排名翻页或换选择不得让项目汇总重新闪烁加载。
- 项目管理检查“全部项目”单一列表、独立项目创建、创建后直达模板、共用原始 LOGO、核销／体验共用说明、充值／退费共用说明、四种横向样例预览和导出，以及保存、封存、激活后的数据库回读；项目页不得出现通用“查询／重置”、活跃／封存分区或文件元数据说明。产品管理另行检查列表页只有“新增产品”入口而没有内嵌表单，独立创建页只有一个产品名称字段；创建后应返回列表并显示自动编号，同时验收活跃／封存和重新激活，且不得出现类别、介绍、模板、配置或删除入口。门店、老师管理检查各自的查询字段、活跃／封存横向表格、独立新增页和独立主页；门店主页逐项检查汇总、四类明细和两类客户分页，老师主页逐项检查账号安全、额度配置、充值、删除和历史。任何创建、模板保存或状态切换只在专用测试数据上提交，并在提交后重新查询数据库确认。
- 充值、退费与产品购买审核分别检查条件查询、精确工单查询、门店／状态筛选、分页、跳页、真实提交／审核时间、工单和客户详情；通过／驳回只在明确可变更的测试工单上提交。
- 总部打开“查询”菜单后必须看到客户、充值、核销三类查询；总部可切换全部／指定门店，门店账号没有“全部门店”能力且服务端始终锁定本店。
- 客户查询逐项验证业务阶段、活跃／封存、建档日期、姓名／生日、上一页／下一页和页码直跳；充值／核销逐项验证项目、业务类型、审核状态、日期、姓名／生日和页码直跳。
- 从首页业务明细、查询列表和客户历史分别点击工单号，必须进入同一张数据库工单；详情页头的完整工单号独占一行且不得换行、截断或省略，状态标签另起一行。核销照片点击后重新鉴权读取原图；相册权限被拒绝后每次再次保存都重新询问是否打开设置，授权成功后自动继续本次保存。
- 老师主页不出现办理门店选择器；建档、充值／退费、产品购买、正常核销和体验核销直接进入完整业务主页面，并在首个正常业务卡内选择门店，不再经过独立前置选店页。未选门店时不读取客户、不启用表单、不允许提交；切换门店会清空上一门店的客户、老师、产品、余额、现场照片与人脸凭证。门店账号不显示选择器，所有客户和单据仍由服务端锁定其绑定门店。
- 选定门店后，“现有客户”必须使用原生下拉框，页面不得直接铺开客户按钮；下拉框启用前按稳定游标自动读完全部活跃客户，并可在下拉框内上下滑动选择，不能永久停在前 100 位。姓名／生日是额外的精确查找入口：只填姓名或只填生日都必须可查，两项都填时同时匹配，两项都空才提示补充；匹配超过 100 位继续分页读完，多位结果按客户编号明确选择。切换门店、模式或查询字段时旧请求必须失效，不能混入新条件。
- 客户建立必须拍照、服务端检测、人脸建立和客户编号返回均成功。
- 充值不做人脸；退费不允许超过已购未退次数。
- 正常核销只读客户剩余大于 0 的项目；体验核销只读当前老师体验剩余大于 0 的项目。选择项目后必须手工填写不超过当前可用数的正整数次数，再进行人脸验证和提交。
- 正常与体验都只对所选客户做 1:1 人脸验证，没有 1:N，没有老师人脸。
- 在提交瞬间断网后重进页面，必须先恢复原结果，不能再扣一次。
- 客户照片加载失败后可单独重读；项目样例和真实工单必须使用同一单据排版，PDF 超过一页时按标准 A4 分页，图片与单张照片保存到系统相册。
