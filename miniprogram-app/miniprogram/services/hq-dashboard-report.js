"use strict";

// Independent mini-program implementation. It intentionally does not import
// the web exporter, although both clients share the same vector PDF contract.
const PAGE_WIDTH = 841.89;
const PAGE_HEIGHT = 595.28;
const MARGIN = 34;
const CONTENT_WIDTH = PAGE_WIDTH - MARGIN * 2;
const ROW_HEIGHT = 22;
const HEADER_HEIGHT = 25;
const COLORS = Object.freeze({
  background: [1, 0.984, 0.953], band: [0.725, 0.541, 0.239], dark: [0.227, 0.18, 0.133],
  muted: [0.404, 0.349, 0.278], pale: [0.957, 0.906, 0.816], paleAlt: [0.99, 0.963, 0.918],
  line: [0.847, 0.773, 0.639], white: [1, 1, 1]
});
function clean(value, fallback = "—") { const output = String(value ?? "").replace(/[\u0000-\u001f\u007f]/g, " ").trim(); return output || fallback; }
function number(value) { const parsed = Number(value); return Number.isFinite(parsed) ? parsed : 0; }
function decimal(value) { return Number(Number(value).toFixed(3)).toString(); }
function color(value) { return value.map(decimal).join(" "); }
function pdfText(value) { let output = ""; for (const character of Array.from(clean(value, ""))) { const point = character.codePointAt(0); output += (point <= 0xffff ? point : 0x003f).toString(16).padStart(4, "0").toUpperCase(); } return `<${output || "0020"}>`; }
function pdfAsciiText(value) { let output = ""; for (const character of Array.from(clean(value, ""))) { const point = character.codePointAt(0); output += (point >= 0x20 && point <= 0xff ? point : 0x3f).toString(16).padStart(2, "0").toUpperCase(); } return `<${output || "20"}>`; }
function textRuns(value) { const runs = []; for (const character of Array.from(value)) { const ascii = character.codePointAt(0) <= 0xff; const previous = runs[runs.length - 1]; if (previous && previous.ascii === ascii) previous.value += character; else runs.push({ ascii, value: character }); } return runs; }
function visualUnits(value) { return Array.from(String(value || "")).reduce((sum, character) => sum + (character.codePointAt(0) <= 0x7f ? 0.56 : 1), 0); }
function fitted(value, width, size) { const source = clean(value); const maximum = Math.max(1, width / Math.max(1, size)); if (visualUnits(source) <= maximum) return source; let output = ""; for (const character of Array.from(source)) { if (visualUnits(`${output}${character}…`) > maximum) break; output += character; } return `${output}…`; }
function safeFilename(value) { return clean(value, "露思卓儿总部业务统计报表").replace(/[\\/:*?"<>|\u0000-\u001f]/g, "-").replace(/\s+/g, " ").slice(0, 100); }
function addFillRect(commands, x, top, width, height, fill) { commands.push(`${color(fill)} rg ${decimal(x)} ${decimal(PAGE_HEIGHT - top - height)} ${decimal(width)} ${decimal(height)} re f`); }
function addStrokeRect(commands, x, top, width, height, stroke = COLORS.line, lineWidth = 0.6) { commands.push(`${color(stroke)} RG ${decimal(lineWidth)} w ${decimal(x)} ${decimal(PAGE_HEIGHT - top - height)} ${decimal(width)} ${decimal(height)} re S`); }
function addText(commands, value, x, top, size = 10, options = {}) { const width = Number(options.width || 0); const output = width ? fitted(value, Math.max(1, width - 10), size) : clean(value); let offset = 0; if (width && options.align === "center") offset = Math.max(0, (width - visualUnits(output) * size) / 2); if (width && options.align === "right") offset = Math.max(0, width - visualUnits(output) * size - 6); let cursor = x + offset; textRuns(output).forEach((run) => { commands.push(`BT /${run.ascii ? "F2" : "F1"} ${decimal(size)} Tf ${color(options.color || COLORS.dark)} rg ${decimal(cursor)} ${decimal(PAGE_HEIGHT - top - size)} Td ${run.ascii ? pdfAsciiText(run.value) : pdfText(run.value)} Tj ET`); cursor += visualUnits(run.value) * size; }); }
function startPage(sectionTitle, continuation = false, metadata = null) { const commands = []; addFillRect(commands, 0, 0, PAGE_WIDTH, PAGE_HEIGHT, COLORS.background); addFillRect(commands, 0, 0, PAGE_WIDTH, 10, COLORS.band); addText(commands, "露思卓儿总部业务统计报表", MARGIN, 24, 18); addText(commands, continuation ? `${sectionTitle}（续）` : sectionTitle, MARGIN, 54, 12, { color: COLORS.band }); let tableTop = 82; if (metadata) { addFillRect(commands, MARGIN, 78, CONTENT_WIDTH, 55, COLORS.paleAlt); addStrokeRect(commands, MARGIN, 78, CONTENT_WIDTH, 55); addText(commands, `时间范围：${clean(metadata.startDate)} 至 ${clean(metadata.endDate)}（Asia/Shanghai）`, MARGIN + 12, 90, 9, { width: CONTENT_WIDTH / 2 - 18 }); addText(commands, `排名对象：${clean(metadata.dimensionLabel)}　排序：${clean(metadata.metricLabel)}`, MARGIN + CONTENT_WIDTH / 2, 90, 9, { width: CONTENT_WIDTH / 2 - 12 }); addText(commands, `项目范围：${clean(metadata.productLabel)}`, MARGIN + 12, 111, 9, { width: CONTENT_WIDTH / 2 - 18 }); addText(commands, `生成时间：${clean(metadata.generatedAt)}`, MARGIN + CONTENT_WIDTH / 2, 111, 9, { width: CONTENT_WIDTH / 2 - 12 }); tableTop = 149; } return { commands, tableTop }; }
function drawTable(commands, columns, rows, top) { let x = MARGIN; addFillRect(commands, MARGIN, top, CONTENT_WIDTH, HEADER_HEIGHT, COLORS.pale); columns.forEach((column) => { addStrokeRect(commands, x, top, column.width, HEADER_HEIGHT); addText(commands, column.label, x + 5, top + 7, 9, { width: column.width - 10, align: column.align || "center" }); x += column.width; }); rows.forEach((row, rowIndex) => { x = MARGIN; const rowTop = top + HEADER_HEIGHT + rowIndex * ROW_HEIGHT; addFillRect(commands, MARGIN, rowTop, CONTENT_WIDTH, ROW_HEIGHT, row.__total ? COLORS.pale : (rowIndex % 2 ? COLORS.paleAlt : COLORS.white)); columns.forEach((column) => { addStrokeRect(commands, x, rowTop, column.width, ROW_HEIGHT); addText(commands, row[column.key], x + 5, rowTop + 6, row.__total ? 9.5 : 8.5, { width: column.width - 10, align: column.align || (column.numeric ? "right" : "left"), color: row.__total ? COLORS.dark : COLORS.muted }); x += column.width; }); }); }
function rowsPerPage(tableTop) { return Math.max(1, Math.floor((PAGE_HEIGHT - tableTop - HEADER_HEIGHT - 38) / ROW_HEIGHT)); }
function tablePages(sectionTitle, columns, rows, metadata = null) { const pages = []; let index = 0; do { const page = startPage(sectionTitle, index > 0, index === 0 ? metadata : null); const size = rowsPerPage(page.tableTop); const chunk = rows.slice(index, index + size); drawTable(page.commands, columns, chunk.length ? chunk : [{ name: "暂无数据" }], page.tableTop); pages.push(page.commands); index += size; } while (index < rows.length); return pages; }
function ascii(value) { const text = String(value); const output = new Uint8Array(text.length); for (let index = 0; index < text.length; index += 1) output[index] = text.charCodeAt(index) & 0xff; return output; }
function concatBytes(parts) { const total = parts.reduce((sum, part) => sum + part.byteLength, 0); const output = new Uint8Array(total); let offset = 0; parts.forEach((part) => { output.set(part, offset); offset += part.byteLength; }); return output; }
function streamObject(value) { const bytes = ascii(value); return concatBytes([ascii(`<< /Length ${bytes.byteLength} >>\nstream\n`), bytes, ascii("\nendstream")]); }
function createPdfBytes(pageCommands) { const objects = new Map(); const pageReferences = []; objects.set(1, ascii("<< /Type /Catalog /Pages 2 0 R >>")); objects.set(3, ascii("<< /Type /Font /Subtype /Type0 /BaseFont /STSong-Light /Encoding /UniGB-UCS2-H /DescendantFonts [4 0 R] >>")); objects.set(4, ascii("<< /Type /Font /Subtype /CIDFontType0 /BaseFont /STSong-Light /CIDSystemInfo << /Registry (Adobe) /Ordering (GB1) /Supplement 5 >> /DW 1000 >>")); objects.set(5, ascii("<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica /Encoding /WinAnsiEncoding >>")); pageCommands.forEach((commands, index) => { const pageNumber = 6 + index * 2; const contentNumber = pageNumber + 1; pageReferences.push(`${pageNumber} 0 R`); addText(commands, `第 ${index + 1} / ${pageCommands.length} 页`, MARGIN, PAGE_HEIGHT - 24, 8, { width: CONTENT_WIDTH, align: "right", color: COLORS.muted }); objects.set(pageNumber, ascii(`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${PAGE_WIDTH} ${PAGE_HEIGHT}] /Resources << /ProcSet [/PDF /Text] /Font << /F1 3 0 R /F2 5 0 R >> >> /Contents ${contentNumber} 0 R >>`)); objects.set(contentNumber, streamObject(commands.join("\n"))); }); objects.set(2, ascii(`<< /Type /Pages /Count ${pageReferences.length} /Kids [${pageReferences.join(" ")}] >>`)); const objectCount = 5 + pageCommands.length * 2; const chunks = [concatBytes([ascii("%PDF-1.4\n%"), new Uint8Array([0xe2, 0xe3, 0xcf, 0xd3]), ascii("\n")])]; const offsets = new Array(objectCount + 1).fill(0); let length = chunks[0].byteLength; for (let objectNumber = 1; objectNumber <= objectCount; objectNumber += 1) { const chunk = concatBytes([ascii(`${objectNumber} 0 obj\n`), objects.get(objectNumber), ascii("\nendobj\n")]); offsets[objectNumber] = length; chunks.push(chunk); length += chunk.byteLength; } const xrefOffset = length; chunks.push(ascii([`xref\n0 ${objectCount + 1}\n`, "0000000000 65535 f \n", ...offsets.slice(1).map((offset) => `${String(offset).padStart(10, "0")} 00000 n \n`), `trailer\n<< /Size ${objectCount + 1} /Root 1 0 R >>\nstartxref\n${xrefOffset}\n%%EOF\n`].join(""))); return concatBytes(chunks); }
function createReportPdf(report = {}) { const productRows = Array.isArray(report.productRows) ? report.productRows : []; const totals = report.totals || {}; const summaryRows = productRows.map((row) => ({ name: clean(row.name || row.productName), verification: String(number(row.verification)), recharge: String(number(row.recharge)), experience: String(number(row.experience)), refund: String(number(row.refund)) })); summaryRows.push({ name: "合计", verification: String(number(totals.verification)), recharge: String(number(totals.recharge)), experience: String(number(totals.experience)), refund: String(number(totals.refund)), __total: true }); const metadata = { startDate: report.startDate, endDate: report.endDate, dimensionLabel: report.dimensionLabel, metricLabel: report.metricLabel, productLabel: report.productLabel, generatedAt: report.generatedAt }; const summaryColumns = [{ key: "name", label: "项目", width: CONTENT_WIDTH - 109 * 4 }, { key: "verification", label: "核销", width: 109, numeric: true }, { key: "recharge", label: "充值", width: 109, numeric: true }, { key: "experience", label: "体验", width: 109, numeric: true }, { key: "refund", label: "退费", width: 109, numeric: true }]; const rankingTotal = Math.max(1, number(report.rankingTotal)); const metric = ["recharge", "verification", "experience", "refund"].includes(report.metric) ? report.metric : "recharge"; const rankingRows = (Array.isArray(report.rankingRows) ? report.rankingRows : []).map((row, index) => ({ rank: String(index + 1), name: clean(row.name), dimension: clean(report.dimensionLabel), recharge: String(number(row.recharge)), verification: String(number(row.verification)), experience: String(number(row.experience)), refund: String(number(row.refund)), share: `${(number(row[metric]) / rankingTotal * 100).toFixed(1)}%` })); const rankingColumns = [{ key: "rank", label: "排名", width: 50, numeric: true }, { key: "name", label: clean(report.dimensionLabel, "分类对象"), width: CONTENT_WIDTH - 50 - 68 - 67 * 4 - 134 }, { key: "dimension", label: "维度", width: 68, align: "center" }, { key: "recharge", label: "充值", width: 67, numeric: true }, { key: "verification", label: "核销", width: 67, numeric: true }, { key: "experience", label: "体验", width: 67, numeric: true }, { key: "refund", label: "退费", width: 67, numeric: true }, { key: "share", label: `${clean(report.metricLabel, "业务")}占比`, width: 134, numeric: true }]; const pages = [...tablePages("项目汇总", summaryColumns, summaryRows, metadata), ...tablePages(`完整排名 · 按${clean(report.metricLabel, "业务")}次数降序`, rankingColumns, rankingRows)]; return { bytes: createPdfBytes(pages), pages: pages.length }; }
module.exports = Object.freeze({ createReportPdf, safeFilename });
