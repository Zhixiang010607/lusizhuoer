# 小程序 0.2.76 上传记录

- 日期：2026-09-24，悉尼时间。
- AppID：`wxb053c1bd6c684d8b`。
- 上传源码基线：`622f7ee62f2ebce32133c9af9d2371bcd51ec2dd`，此前已推送至 `origin/main`。
- 版本说明：接入魔法柔肤 `LASER-BLE` 扫码开机，支持 `LA` 设备编号及广播名格式，并保留旧 `NCM` 设备兼容。

官方微信开发者工具已成功构建小程序 NPM；构建只出现既有的 `bson` 入口提示，没有新增编译错误。第一次上传于悉尼时间 **20:26:43** 进入后台，完成编译和打包后停在 COS 传包阶段，持续约 35 分钟仍没有 `doUpload:await`、`progressSuccess` 或失败终态。为清除已经卡死的后台任务，随后退出并重启开发者工具。

第二次上传于悉尼时间 **21:02:26** 发起，约 8 秒完成编译和打包并进入 `doUpload:start`；客户端连接随后返回 `code 10 / socket hang up`。该错误不是成功回执，也不能单独证明后台任务已经停止。只有后续出现原生后台 `progressSuccess`，或公众平台版本列表明确显示 `0.2.76`，才允许把本版记为已上传。

第三次上传改用开发者工具原生“上传”界面，于悉尼时间 **21:07:42** 发起，版本号明确填写为 `0.2.76`，更新类型选择“特性更新”，备注为本页所列版本说明。编译、检查和打包约 1 秒完成，随后进入 COS 传包；开发者工具在悉尼时间 **21:12:42** 明确显示“上传失败”，错误为 `Error: Timeout [2.02.2608040][darwin-arm64]`。该次失败终态已由界面和本地开发者工具日志共同确认。

第四次原生界面操作误选“特性更新”后，工具把版本号自动改为 `0.3.0`。该任务于悉尼时间 **21:34:34** 进入 COS 上传，但在 **21:35:00** 前主动退出开发者工具终止；日志没有 `progressSuccess`，因此该次不计为有效的 `0.2.76` 上传，也不能视为 `0.3.0` 已上传。

第五次重新打开原生上传界面，明确选择“修订补丁”，版本字段为 `0.2.76`。任务于悉尼时间 **21:49:08** 开始，约 3.5 秒完成编译、代码检查和打包，**21:49:12** 进入 COS 传包；悉尼时间 **22:05:52** 返回官方后台终态 `progressSuccess`，总耗时 `1,004,281` 毫秒。开发者工具显示的“构建 100”是日志条数；本次编辑器错误数和问题数均为 0，质量扫描中的主包体积、资源体积和依赖位置提示没有阻止上传。该次原生界面的项目备注沿用了此前公司主页说明，未准确概括本次 BLE 变更；它不影响实际上传包体，后续设为体验版或提交审核时必须以本页顶部的 `LASER-BLE` 版本说明为准。

截至本记录更新时，**`0.2.76` 已取得成功回执**，当前最后确认上传成功的开发版为 `0.2.76`。本版没有提交审核、没有正式发布，体验版也未回读。真实 iOS／Android 手机与实体 `LASER-BLE` 设备的扫码、连接、HMAC 授权、回执恢复和核销闭环仍待联合验收。

本轮与客户端配套的生产后端状态单独计算：迁移 069 已执行并取得 3 行 `READY`；`faceRecognition v114` 已部署为 `Active`，线上文件与仓库一致，运行时 `health` 返回 `ok=true`、`version=v114`。

- [脱敏上传状态](evidence/0.2.76/backend-upload-status.log)
- [迁移 069](../../database/cloudbase-console/069-01-magic-soft-skin-ble-identity.sql)
- [迁移 069 只读验证](../../database/cloudbase-console/069-readonly-verify.sql)
- [上一版上传记录](0.2.75.md)
