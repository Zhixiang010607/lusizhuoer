# 小程序 0.2.71 开发版本上传记录

- 日期：2026-09-07，悉尼时间。
- AppID：`wxb053c1bd6c684d8b`。
- 待上传源码：`b69450d8c184a0858b43960bdf51330c332b8cc5`，已推送至 `origin/main`。
- 包含登录等待优化 `307cd21` 和中文项目介绍／返回按钮优化 `c67b9de`。

**本版已于悉尼时间 16:03:16 完成后台上传。** 官方 CLI 在约五分钟时先返回 `code 10 / Timeout`，但后台没有停止；保留该任务继续运行后，官方上传日志最终依次记录 `parseError`、`progressSuccess` 和 `reportNewRemoteDebug`，编译及上传共耗时 `1,808,795` 毫秒。不能再把本次 CLI 超时当作最终上传失败。

此次成功证据来自原生工具后台的终态日志；已经结束的 CLI 没有补发 `✔ upload` 或包体 JSON。版本号 `0.2.71` 来自该次官方上传命令，后台版本号未单独回读。上传后再次打开官方上传入口，工具提示“上次提交已被选为体验版，本次上传将会覆盖体验版”；已确认该关联提示，体验版具体版本号及手机打开效果尚未独立验收。没有提交审核或正式发布。

本版为登录页接入三个项目介绍，保留原配色，支持上下滚动、章节切换、图解动效和返回登录；三个项目使用完整中文说明，返回按钮放大并加粗。登录流程减少一次重复的云端用户资料读取，既有身份同步、UID 对比及服务端权限复核继续保留。

324 项仓库测试通过，三个项目已经完成 320 × 568 与 430 × 932 原生模拟器文案、导航及边界检查；物理手机验收尚未完成。详见[中文介绍验收](../../docs/design/project-intro-native/validation-chinese.md)和[登录等待检查](../../docs/reviews/2026-09-07-mini-login-speed.md)。本次再次执行官方 `build-npm` 成功，仅保留既有 `bson` 入口警告；登录页已正常编译，原生调试器显示 0 个错误、4 个警告。

## 交替尝试记录

用户明确要求发布，并要求预览版、体验版依次尝试。官方 `wechatide upload` 返回等待确认，但项目主窗口没有显示对应确认框，不能把该返回当成上传成功。

原生开发者工具的上传入口随后明确提示：“上次提交已被选为体验版，本次上传将会覆盖体验版”。界面填入版本 `0.2.71` 和本版更新说明后开始上传，约五分钟后返回：

```text
上传失败
Error: Timeout [2.02.2608040][darwin-arm64]
```

该提示确认了现有开发提交与体验版的关联，但当时不能证明本版已经更新。退出开发者工具、确认本地服务端口已关闭后，再通过官方 CLI 单独发起手机扫码预览。约五分钟后同样返回 `code 10 / Error: Timeout`，没有生成新二维码或包体回执。该命令进程退出码为 0，不能覆盖其明确的客户端超时结果。

15:25—15:30 还在隔离目录中，用 14:00 曾成功预览的 `efb340b` 源码做对照；162 个已跟踪客户端文件逐个匹配该提交，配置及依赖清单一致，对照预览仍超时。这只能说明超时不依赖本轮三处运行时代码修改，不能证明所有代码或网络条件均无问题。该旧源码只用于对照预览，没有覆盖开发版本。

15:33 改用此前成功过的官方 CLI 上传，临时将工具从系统代理改为直接连接。编译及打包仅耗时 `2,299` 毫秒，实际上传临时文件为 `4,246,718` 字节。CLI 超时后仍观测到上传连接发送数据及重传；只读核对安装包内的消息等待逻辑，确认五分钟超时会删除等待回调，但该计时器不发送后台取消指令。因此保留此轮后台任务，最终于 16:03:16 取得成功终态。网络计数包含重传且可能随连接切换而重置，不能当作有效上传百分比。

工具内置的 COS 上传异常处理包含重试及备用上传路径；本次没有修改工具程序、认证、系统网络配置或调用非公开上传接口。没有取得足以确定每次网络重试原因的错误回执，不能断言代理、某个服务器或具体网络条件就是唯一根因。上传结束后，工具代理已恢复原来的“使用系统代理”。

按照用户要求，本机临时启用四小时屏幕及系统保持唤醒，未更改小程序源代码、依赖或认证规则。保持唤醒后原生上传仍返回超时，不能仅将上传失败归因为锁屏或凭证过期。

重新刷新模拟器后，错误过滤结果为空，并取得最新登录入口及介绍页尾截图；实际点击项目卡片、滚动到页尾、点击大字返回按钮以及顶部返回入口均通过，没有登录真实账号或写入业务数据。

16:06:23 又通过官方 `auto-preview` 发起自动预览。客户端同样在约五分钟时返回 `code 10 / Timeout`，保留后台任务继续运行后，已于 **16:33:13.819** 取得 `parseError`、`progressSuccess` 和 `reportNewRemoteDebug`，编译及上传共耗时 `1,606,917` 毫秒。这证明官方自动预览后台请求已成功处理；手机是否收到、显示的版本以及实际交互尚未独立验收。自动预览路径没有生成新的扫码二维码，CLI 也没有补发包体 JSON。14:00 的历史二维码仍不作为最新版交付。

最后确认后台上传成功的开发版本现为 `0.2.71`。没有新 SQL、云函数或静态网页部署。二维码、原始调试日志和本地任务信息只保存在被 Git 忽略的输出目录；本记录仅提交脱敏后的阶段日志和不含用户资料的登录截图。

- [本轮上传与预览结果](evidence/0.2.71/upload-result.json)
- [后台成功终态日志](evidence/0.2.71/backend-upload-completion.log)
- [自动预览后台成功日志](evidence/0.2.71/backend-auto-preview-completion.log)
- [最新登录入口截图](evidence/0.2.71/login-latest.jpg)
- [项目介绍页尾截图](evidence/0.2.71/ocean-footer-latest.jpg)
- [上传后模拟器复查](evidence/0.2.71/post-upload-simulator-qa.json)

## 可直接填写的版本说明

新增三个项目的中文介绍、滚动动效和图解；补充项目特点，优化返回登录按钮，并减少登录流程中的重复资料请求。
