# 露思卓儿业务系统：项目规则与跨设备交接基线

> 最后整理：2026-08-26
>
> 适用仓库：`https://github.com/Zhixiang010607/lusizhuoer`
>
> 当前分支：`main`

## 0. 这份文件怎么用

这份文件是另一台电脑、新 Codex 任务或新开发人员接手项目时必须首先读取的项目基线。它记录的是经过多轮讨论后仍然有效的最终业务规则，而不是按聊天时间堆叠的原始需求。

执行规则：

1. 开始开发前先完整阅读本文件，再阅读根目录 `AGENTS.md`、`README.md` 和相关云函数 README。
2. 本文件中的“最终规则”和“明确废弃”优先于旧截图、旧迁移名称、旧 README 段落和历史实现。
3. 数据库、云函数、网页和小程序必须遵守同一套服务端业务规则；前端显示从来不是安全边界。
4. 如果代码与本文件不一致，先报告差异，再修复代码或更新本文件；禁止悄悄选择其中一个版本。
5. 新需求一旦推翻旧规则，必须在同一个提交中更新本文件，不能继续把相互矛盾的规则叠加在 README 末尾。
6. 本文件不得写入 SecretId、SecretKey、API Key、数据库密码、真实手机号、人脸文件地址或清理令牌。

## 1. 产品目标与客户端范围

系统用于总部、门店和老师共同管理美容项目、涂抹类／实物产品、客户、充值、退费、正常核销、老师赠送的体验核销、业务凭证、审核和数据汇总。

当前客户端方向：

- 总部：继续保留网站，并逐步提供小程序能力。
- 门店：网站与微信小程序；移动端是重点。
- 老师：网站与微信小程序；移动端是重点。
- 原生 App：未来可能增加，但当前不复制后端业务逻辑。
- 小程序和未来 App 必须复用现有 CloudBase 云函数和 PostgreSQL，不另建一套余额、权限或核销规则。
- BLE 是小程序／App 的后续阶段。当前核销成功后先写入服务端设备信号 outbox；不能在数据库事务尚未确认成功前开机。

## 2. 架构与权威边界

当前系统由以下部分组成：

- 静态网站前端：仓库根目录的 HTML、CSS 和 JavaScript。
- 原生微信小程序：`miniprogram-app/`。
- 未来原生 App：只允许建立在独立的 `native-app/`；当前只有边界说明，没有 App 运行代码。
- 账号与管理云函数：`cloudfunctions/staffAccount/`。
- 客户、人脸和业务云函数：`cloudfunctions/faceRecognition/`。
- 核销照片云函数：`cloudfunctions/verificationPhoto/`。
- 老师创建云函数：`cloudfunctions/teacherCreate/`。
- 权威数据与事务：CloudBase PostgreSQL。
- 私有照片对象：CloudBase 私有存储桶。

不可破坏的边界：

- 余额、体验额度、角色、门店范围、工单归属、客户访问权、照片访问权和防重结果全部由服务端重新判断。
- 浏览器或小程序传入的 `role`、`storeId`、`teacherId`、`accountId`、次数、审核状态均不能直接信任。
- 每次调用都按当前 CloudBase UID 回读账号、角色、状态和门店范围。
- 关键业务以数据库事务和数据库约束为最终真相，前端成功提示不能替代数据库确认。

### 2.1 客户端物理隔离

- 当前根目录 HTML、CSS、JavaScript 统一视为旧版网页客户端；原生微信小程序只位于 `miniprogram-app/`。
- 未来原生 App 只允许建立在独立的 `native-app/`，不得放进网页根目录或 `miniprogram-app/`。
- `cloudfunctions/` 与 `database/` 是各端共享的服务端边界；共享的是服务端业务语义和接口契约，不共享客户端 UI、路由、会话存储、平台 API、依赖或构建产物。
- 单端需求默认只修改目标客户端。共享接口确需变化时，必须先列出受影响端，保持兼容或同步升级，并分别回归网页、小程序和未来 App，禁止为了一个端临时改坏另一个端。
- 将现有网页迁入 `web-app/` 必须作为独立迁移处理，单独验证静态托管、离线入口、资源路径和测试，不与普通功能修改混合。

## 3. 身份、账号与人员模型

### 3.1 全局账号规则

- 一个中国大陆手机号只能绑定一个系统业务身份。
- 业务身份只有总部、门店、老师三种；运营身份已经退出系统。
- 手机号是外部唯一账号标识。同一个自然人是否使用过其他人脸不参与账号唯一性判断。
- 一个人可以拥有多个老师账号，但每个账号必须使用不同手机号；系统不尝试判断这些账号是否属于同一个自然人。
- 手机号一旦绑定，不允许注册成另一角色或生成第二个业务账号。
- 手机号＋密码、手机号＋短信验证码和微信小程序手机号授权登录的是同一个 CloudBase Auth 账号和同一 UID，不产生第二身份。
- 微信快捷登录必须由用户点击手机号授权按钮明确发起；不得在启动时自动申请手机号，不得把输入框手机号或客户端传入的手机号当作身份证明。
- CloudBase 只允许把微信已验证手机号关联到同号的既有 Auth 用户，并必须保留该用户原 UID；陌生手机号不得自动注册 Auth 用户、员工账号或业务身份。
- 所有 `session` 与业务请求只使用平台验证后的当前 CloudBase UID 回读业务账号；禁止根据客户端手机号自动新建、替换或修复 `staff_accounts.auth_uid`。UID 未命中既有员工账号时必须失败并退出 CloudBase 会话。
- 密码只保存安全哈希；初始密码只用于首次或应急登录。
- 重置密码、封存、激活和关键权限变更必须保留操作人、时间和原因审计。

### 3.2 老师账号最终规则

- 创建老师只需要姓名、手机号和初始密码。
- 老师创建不拍照、不上传照片、不创建人脸库 Person、不保存 FaceId。
- 老师人脸与老师账号创建、激活、登录、充值、退费、正常核销和体验核销均无关。
- 老师创建成功的服务端条件是 Auth 用户、员工账号和老师主档全部创建并回读一致；任何一步失败都显示失败并清理本次残留。
- 老师主档和登录账号创建后直接为活跃状态；不得再出现“缺少老师人脸所以不能激活／接单”的门禁。
- 老师详情和老师创建页不再提供补录、替换或删除老师人脸入口。
- 历史老师人脸 Saga、委托动作、租约、清理墓碑和兼容入口已经废弃，不得恢复。

### 3.3 门店账号

- 门店创建填写门店名称、完整省市区与详细地址、联系人及电话。
- 创建后生成门店编号和登录账号。
- 门店登录账号只能代表其绑定门店，不能通过 URL、下拉框或请求参数切换到其他门店。

### 3.4 总部账号

- 总部负责全局查看、管理、配置和审核。
- 总部不是一线业务提交者，不能创建客户、提交充值、提交退费、办理正常核销或办理体验核销。
- 总部历史提交的旧工单继续保留审计，但不得因此归属于某个老师。

## 4. 角色权限矩阵

### 4.1 总部允许

- 查看全局、门店、项目、老师和客户统计。
- 查看客户、充值、退费、核销和体验历史详情。
- 审核充值和退费申请。
- 创建、封存和激活门店、项目、独立产品、老师及其账号。
- 配置老师的体验项目基础额度和单独充值体验次数。
- 在有权限的管理页重置账号密码。
- 查看完整排名和分页数据。
- 审核门店或老师提交的独立产品购买单；总部仍不能代替一线账号提交购买。

### 4.2 总部禁止

- 禁止客户建立。
- 禁止提交办卡充值。
- 禁止提交退费申请。
- 禁止正常核销。
- 禁止体验核销。
- 前端必须移除这些入口，云函数也必须拒绝直接绕过页面调用。

### 4.3 门店允许

- 在本门店建立客户。
- 为本门店客户提交充值申请；业务老师可选，可以留空。
- 为本门店客户提交退费申请；业务老师可选，可以留空。
- 为本门店客户办理正常核销；必须选择一位真实且活跃的业务老师。
- 查看本门店客户、工单、统计、活跃客户和封存客户。
- 将本门店客户封存或恢复；不删除历史记录。
- 为本门店活跃客户提交独立产品购买申请；不选择业务老师，审核通过后才计入客户产品汇总。

### 4.4 门店禁止

- 禁止体验核销；体验只能由老师账号赠送。
- 禁止切换或伪造其他门店。
- 禁止进入总部审核和总部管理页面。
- 禁止删除客户或修改客户姓名、生日、人脸、历史余额与审计记录。
- 门店不能通过选择业务老师把原提交账号的改单、恢复或补照片权限转交给老师。

### 4.5 老师允许

- 登录后进入个人工作台。
- 办理业务前选择一个真实且活跃的门店；该门店写入本次工单。
- 建立客户、提交充值、提交退费、办理正常核销和办理体验核销。
- 查看与自己存在有效业务关系的客户主页及该客户全部历史业务详情。
- 查看自己的体验项目剩余次数、业务汇总、业务明细和关联客户。
- 选择真实活跃门店后，为该门店活跃客户提交独立产品购买申请；服务端自动绑定当前老师本人。

### 4.6 老师禁止

- 禁止访问无关客户。
- 禁止审核充值或退费。
- 禁止进入门店／老师／项目／总部管理页。
- 禁止管理客户状态。
- 禁止把业务改绑到其他老师。
- 禁止修改、作废、撤销或补充不是自己提交的工单；跨老师历史只读。

## 5. 客户建立与客户人脸

- 客户建立必须填写姓名和生日，并绑定发生门店。
- 客户建立必须现场拍摄客户本人照片，取得明确授权，并由服务端进行质量和活体检测。
- 质量、活体、原始照片上传、人脸 Person／Face 创建、数据库客户主档和照片引用写入全部成功后才算客户建立成功。
- 建立结果必须回读并确认客户编号、人脸标识和照片引用；失败不得显示创建成功。
- 客户人脸用于核销时的 1:1 验证，不用于登录。
- 系统从来不做 1:N 人脸搜索；先明确选择客户，再将现场照与该客户档案做 1:1 比对。
- 客户建档照缺失或读取失败时不得确认客户、不得核销。
- 同名同生日客户用客户编号区分；显示名称可以加顺序号帮助操作员区分，但数据库原姓名不随显示序号改变。
- 客户所属门店、编号、姓名、生日、人脸、照片和审计字段是受控主数据，不能由普通门店或无关老师随意修改。

## 6. 业务提交和自动绑定

### 6.1 共同规则

- 总部不能提交业务。
- 门店账号提交时，门店由登录 UID 自动锁定；请求中即使传入其他门店也必须忽略或拒绝。
- 老师账号提交时，老师由登录 UID 自动锁定，不需要再次填写老师；请求中传入其他老师必须忽略或拒绝。
- 门店充值和退费的业务老师可选；留空时该工单不归属任何老师，选择时服务端必须验证老师真实且活跃。
- 门店正常核销的业务老师必选；门店没有体验核销权限。
- 老师账号的充值、退费、正常核销和体验核销都必须绑定当前登录老师本人；界面可以显示“自动绑定”，但不能让老师改选别人。
- 老师账号办理前选择本次发生门店；服务端验证该门店真实且活跃。
- 每张工单保留发生时门店名称、项目名称、客户名称及业务老师名称快照，避免后续改名破坏历史展示。
- 客户主页中的充值、正常核销、体验核销和退费记录均显示“业务老师”列：有经过服务端验证的 `teacher_id` 时显示对应老师，允许留空的门店充值／退费未选择老师时显示“未指定”。

### 6.2 业务老师的唯一认定口径

- 工单业务归属使用经过服务端角色矩阵和活跃状态校验后写入的 `teacher_id`；页面当前老师、查看者老师或客户端临时字段不能替代数据库值。读取历史归属时还必须验证提交来源与业务类型：门店提交只在充值、退费和正常核销采用其所选老师，老师提交只在充值、退费、正常核销和体验核销归到该账号绑定的本人；总部、已退役 `operation`、老师账号与 `teacher_id` 不一致、门店体验或历史补录工单不得产生老师归属。
- 门店充值／退费可以没有 `teacher_id`；门店正常核销必须有 `teacher_id`；门店不得创建体验核销。
- 老师账号提交的四类业务必须由服务端把 `teacher_id` 锁定为当前老师本人。
- 有效／已通过工单的 `teacher_id` 计入该老师业务统计并建立相应客户关系，即使真实提交账号是门店。
- `submitted_by_account_id` 始终记录真实操作账号，并继续决定防重恢复、修改、补照片和其他写权限；成为业务老师只获得规定的统计与只读客户关系，不获得原提交人的写权限。
- 未选择老师的门店充值／退费不计入任何老师；总部仍禁止新提交业务。历史总部、已退役 `operation`、老师账号与 `teacher_id` 不一致、门店体验或补录工单仍保留业务总量与审计记录，但老师维度一律视为“未指定”，不得建立老师客户关系、直接照片读取权或老师统计归属。

## 7. 充值

- 充值需要先确认客户、门店和项目，再填写次数及可选留言。
- 正常充值可以附带可选的实物／涂抹类产品赠予；退费不能附带赠品。第三步只读取 `public.retail_products` 中当前为 `ACTIVE` 的产品，操作顺序固定为“选择激活产品 → 填写 1—999 的正整数数量 → 点击＋加入清单”；一单最多 20 种，同一产品只能加入一次，提交前可以逐条删除。
- 赠品清单必须与充值主单在同一数据库语句中原子写入，并绑定同一客户、发生门店和该充值单最终确定的业务老师；门店充值未选老师时赠品老师允许为空，老师账号提交时继续自动绑定本人。服务端必须在写入瞬间重新检查产品仍为激活，并固化产品编号和名称快照。
- 已写入工单的赠品明细属于不可变审计，产品之后封存也不得抹除历史。赠品不创建客户项目次数、客户余额、老师体验额度、库存或总部四类业务统计；它只随父充值工单显示和导出。
- 充值不做人脸识别，不采集现场人脸照片。
- 门店和老师可以提交；总部不能提交。
- 充值申请进入总部审核，只有审核通过后影响客户有效余额。
- 审核点击“通过／驳回”并完成当前操作即可，不再弹出多余的第二层确认窗口；服务端安全、权限、审计和防重逻辑必须保留。
- 已通过充值的作废／撤销必须走受控申请和审核，不直接删除或改写原单。
- 客户余额和统计必须区分充值、核销、体验、退费，不把不同事件用一个净数掩盖。

### 7.1 独立产品购买

- 独立产品购买只使用 `public.retail_products` 中当前激活的涂抹类／实物产品，与项目充值完全分开。
- 顶部“办”入口名称固定为“产品购买”，门店和老师可提交，总部不提交；老师每次先选门店，门店账号自动锁定本店。
- 填报顺序复用充值的客户确认方式：全量活跃客户原生下拉框或姓名＋生日精确查找，确认客户后选择一个激活产品、填写 1—999 件整数数量和可选留言。产品购买不做人脸识别。
- 产品购买使用独立 `PP...` 工单、独立数据库表和独立总部“产品购买审核”入口，不混入充值／退费审核。只有总部审核通过后才进入客户汇总；驳回和待审核均不计入。
- 客户主页增加三列“产品名称／购买／赠送”汇总。“购买”累计审核通过的产品购买件数；“赠送”累计父充值单审核通过的赠品件数。两者都不改变项目余额、核销、老师体验额度、库存或总部四类项目业务统计。
- 老师提交并审核通过的产品购买可建立该老师对客户的只读业务关系；原提交账号仍决定防重恢复和写权限。

## 8. 退费

- 门店和老师可以为有可退次数的客户提交退费申请；总部不能提交。
- 退费不能超过服务端计算的可退次数，不能依赖前端显示余额。
- 退费进入总部审核，只有审核通过后影响客户余额。
- 退费审核与充值审核一样，不使用多余的二次弹窗，但保留服务端权限、审计、幂等和状态机。
- 报表分别显示有效充值、正常核销、体验和有效退费，不能通过简单相减猜测业务原因。
- 客户余额由权威余额状态机按已通过工单计算；体验核销永远不减少客户购买余额。

## 9. 正常核销

- 门店和老师可以办理正常核销；总部不能办理。
- 先选择并确认客户，再选择客户剩余次数大于零的项目。
- 必须现场拍摄客户，完成活体和该客户档案的 1:1 人脸验证。
- 核销成功在数据库事务中原子扣减客户项目余额并写入完整照片与审计关系。
- 数据库确认成功后才允许生成／发送设备开启信号。
- 核销一经成功不提供普通撤销或删除入口；误操作通过新的受控补偿业务处理，不能反向改写原始核销证据。

## 10. 老师体验核销

- 体验核销只能由当前登录老师发起。
- 门店和总部没有入口，服务端也必须拒绝调用。
- 体验核销自动绑定当前登录老师，不要求再次扫描或选择老师。
- 现场扫描、活体和 1:1 比对的对象是客户，不是老师。
- 体验凭证的固定照片全部是客户照片，不是老师照片。
- 体验核销扣减当前老师、当前项目的体验额度，不扣客户购买余额。
- 体验项目下拉框只显示该老师已配置、项目活跃且当前可用次数大于零的项目。
- 体验额度扣减、核销工单、照片绑定、审计和设备信号必须处于同一可靠业务链；任何必要步骤失败都不得开机。
- 历史曾经使用老师照片生成的旧体验单按真实历史保留，不伪装成客户照片；新单一律使用客户照片。

## 11. 老师体验额度

- 老师体验额度与客户购买余额完全独立。
- 每个老师可有多个体验项目，界面必须按实际项目数量自适应。
- 总部可按老师＋活跃项目配置月度基础次数。
- 每个老师每个项目只有一份当前活跃基础配置。
- 保存新配置立即生效，并将当前可用次数直接覆盖为新值；不累加旧余额，也不按当月剩余天数折算。
- 已配置项目允许删除配置；删除是封存，不删除历史体验、配置、月初重置或充值记录。删除后可以重新配置。
- 总部可以为某位老师的某个已配置项目单独充值体验次数；单独充值在当前余额上增加，并保留备注和不可变流水。
- 每月上海时区 1 日 00:00，仅对活跃老师、活跃项目和活跃配置执行月度基础额度重置。
- 老师主页显示每个活跃体验项目当前剩余次数和累计体验次数；累计业务次数只统计来源可信、已生效且由该老师本人提交的体验核销。历史异常扣减仍留在额度余额与不可变流水中供审计，但不得伪装成老师业务统计。

## 12. 核销照片、查看、导出和下载

### 12.1 固定证据

- 正常核销和新体验核销共用同一套客户照片保存流程。
- 每张新核销至少固化两张不可修改的核心证据：客户建档留存照、客户本次现场照。
- 另有三个补充照片位，因此详情页固定展示五个照片位。
- 照片未拍摄、上传失败、数据库引用绑定失败或固定证据不完整时，核销不能成功。

### 12.2 补充照片写权限

- 只有真实原提交账号可以在服务端截止时间内上传或替换补充照片。
- 当前规则为工单提交后 24 小时内；服务端时间为准。
- 其他老师、其他门店和总部不能代为补照片；跨老师查看只有只读权限。
- 每单同一时间最多一个进行中的补充照片任务，防止多标签页和慢请求覆盖。

### 12.3 读取可靠性

- 列表和首屏优先加载缩略图，点击单张后再鉴权读取高清原图。
- 临时签名地址失效、滑动后图片消失或首次加载失败时，单张照片必须提供“重新读取”机制。
- 重新读取必须再次验证当前账号对该工单的权限，不能复用过期权限绕过服务端。
- 首屏清单不能因为某张缩略图暂不可用而批量读取五张原图或原图字节；数据库声明存在的照片位必须保留独立重载状态。点击哪张只重新鉴权并读取该张安全原图，其他已经显示或正在加载的照片不得刷新。单张回退成功后先写入小程序本地临时文件再显示，不能把大段 Base64 持久化或反复放进页面状态。
- 页面不能把签名 URL 或原图持久写入浏览器存储。
- 为控制内存，可以释放离屏高清 Blob，但再次点击必须能单独重读。

### 12.4 导出和本地保存

- 核销详情允许导出完整 PDF 或图片，也允许单张照片下载／保存到本地。
- 导出包含全部五个照片位。数据库明确为空的照片位可显示空占位；数据库声明存在的照片必须全部成功读取和解码，否则取消导出并提示重试。
- 不得因为临时地址失效而生成缺照片的“成功文件”。
- 下载和保存使用服务器保存的高清原图，不做第二次压缩；缩略图只用于界面预览。
- 手机端下载使用平台原生“保存到相册”能力，让照片直接进入系统相册。
- 浏览器摄像头打不开时应明确区分 HTTPS、安全权限、系统隐私权限、设备占用和浏览器兼容问题，不把相机失败误报成业务失败。

## 13. 客户访问权和跨老师历史

### 13.1 老师何时获得客户主页权限

老师满足以下任一关系即可获得该客户主页权限：

- 当前老师账号亲自创建该客户；
- 该客户已生效充值的 `teacher_id` 是当前老师；
- 该客户已生效退费的 `teacher_id` 是当前老师；
- 该客户有效正常核销的 `teacher_id` 是当前老师；
- 该客户有效体验核销的 `teacher_id` 是当前老师。

无效、拒绝、撤销、未选择当前老师或仅由别人提交且未归属当前老师的业务不能凭空建立老师关系。

### 13.2 获权后可以查看什么

- 可以进入客户主页并读取客户建档照片。
- 可以查看该客户由任何老师、门店或历史总部账号提交的全部充值、退费、正常核销和体验核销详情。
- 可以查看有权限的核销照片和审计信息。
- 可以查看客户备注，并按产品设计追加老师留言或保存允许编辑的业务备注。

### 13.3 获权后仍然不能做什么

- 不能因为能看客户主页就修改其他提交人的工单。
- 不能为其他提交人的工单补照片、作废、撤销或改写原始证据。
- 不能修改客户姓名、生日、所属门店、人脸和历史余额。
- 不能封存或恢复客户；客户状态管理仍属于绑定门店和总部权限。

### 13.4 门店客户范围

- 门店客户列表以客户绑定门店为主，并结合本店有效业务。
- 门店只能查看自身范围，不能通过老师关系扩大到其他门店。
- 门店创建的客户不会自动绑定某个老师。

## 14. 老师主页

- 老师主页基础信息不显示老师人脸状态，也不以人脸决定激活状态。
- 基础信息下自适应显示所有活跃体验项目及剩余次数。
- 顶部统一时间范围：今天、本周、本月、本季度、本年、全部、自定义；默认今天。
- 时间范围同时控制业务汇总和紧随其后的业务明细，不能各用一套日期。
- 汇总采用项目为行、核销／充值／体验／退费为列的矩阵，并显示四类总数。
- 业务明细紧接在时间汇总下方，分为核销、充值、体验和退费四个可切换按钮。
- 明细很多时服务端分页；桌面保留上一页、下一页、页码和直接跳页。
- 手机端每条业务仍保持一行，在明细容器内部横向滑动；不能把整个页面撑宽。
- 老师业务汇总、明细和客户关系按有效工单 `teacher_id` 归属；门店提交但明确选择该老师的业务也计入，未选择老师的门店充值／退费不计入。
- 被门店选择为业务老师不转移工单写权限；非原提交老师对该工单仍然只读。
- 页面之后分别显示“活跃用户”和“封存用户”。
- 老师用户列表只显示姓名、门店、生日、该老师有效充值次数和正常核销次数；不单列体验和退费次数。
- 客户姓名必须是进入客户主页的链接。

## 15. 门店主页

- 门店主页业务区与老师主页使用相同布局、字号、日期选择、汇总矩阵、明细位置、分页和手机端横向滚动规则。
- 门店主页默认时间同样为今天。顶部档案只显示门店名称、状态和业务编号；下方不再重复唯一身份 ID、门店名称、门店状态或业务编号，只以暖象牙色 2×2 信息格显示地区、详细地址、联系人和联系电话。
- 门店业务办理区不显示体验核销入口，门店账号也不能调用体验创建接口；老师在该门店完成的历史体验仍作为只读审计记录保留在本店汇总、客户历史和总部查询中，不能因入口下线而删除或改写。
- 门店主页不显示老师专属体验剩余额度卡。
- 门店主页的统计按本门店业务与绑定客户计算，不套用老师个人提交人口径。
- 页面最后分别显示本门店活跃用户与封存用户。
- 门店客户列表与老师客户列表列结构一致：姓名、门店、生日、有效充值次数、正常核销次数。
- 即使所有客户门店相同，也必须保留门店列。
- 客户姓名保留客户主页链接。

## 16. 总部页面和管理页

- 总部侧栏不显示任何业务办理按钮。
- 老师管理、门店管理和其他移动列表在手机端使用横向单行布局；不能把一个记录拆成难以识别的多行卡片。
- 总部老师管理的操作颜色与客户封存操作统一，不使用刺眼、含义混乱的红蓝组合。
- 只有总部进入门店主页可以封存／激活门店；其他角色无权操作。
- 不需要门店业务汇总 PDF 导出入口；单笔充值／核销凭证导出仍保留。
- 运营管理、运营账号创建和运营主页全部移除。

## 17. 查询、审核和分页

- 充值审核和退费审核数据量大时必须服务端分页，不能无限增长页面高度。
- 核销查询、充值查询、客户查询已经分页，并必须保留直接跳转页码。
- 核销查询不显示“全部／待审核／已通过／已驳回”审核状态汇总卡；正常与体验核销没有审核流程，结果总数由结果卡右上角统一显示。充值／退费查询仍可保留审核状态汇总。
- 充值审核、退费审核也提供上一页、下一页、页码和直接跳页。
- 手机端表格在自己的容器内横向滑动，标题和固定区域不能因内容横滑跑出页面或停在错误位置。
- 单条记录保持一行；只有确实宽度不足时才在容器内横向滚动。
- 所有查询结果中的客户、门店、项目、老师和工单编号在有对应详情页时应提供链接。

## 18. 数据统计口径

- 统计事件分为四类：有效充值、有效正常核销、有效体验核销、有效退费。
- 四类事件分别展示，不互相抵消后只显示一个净值。
- 体验核销不进入客户购买余额消耗，但进入体验统计和老师体验额度消耗。
- 只有当前有效／已通过的业务计入业务汇总；拒绝、待审、无效或已正确作废的记录按状态机规则排除。
- 客户、老师、门店或项目后来封存不会追溯删除其历史有效业务。
- 统计时间按上海时区自然日。
- 本周为周一至今天；本月为 1 日至今天；本季度按自然季度；本年为 1 月 1 日至今天。
- 已结束季度显示完整季度，当前季度截止今天，未开始季度无数据。
- 自定义范围最多 366 天；“全部”必须明确请求全历史，不能把缺少日期误认为全部。
- 总部首页不显示 Top 10 分类图；门店／老师完整排名直接由服务端分页返回。
- 完整排名默认每页 100 条并支持跳页；完整矢量 PDF 导出按每批 500 条读取项目汇总和排名，避免 CloudBase 6 MB 响应上限，单次各最多 10,000 条。
- 门店／老师排名的候选集合按所选日期和项目范围确定：当前为 `ACTIVE` 的门店／老师即使本期四项业务均为 0 也必须参与；当前已封存者只有在该范围内存在至少一条有效充值、核销、体验或退费时才参与。封存不得抹除历史事实，也不得把本期无业务的封存主档作为 0 次排名项。
- 大数据接口不得返回全部明细后让浏览器自己筛选。

## 19. P0：防重复写入和超时恢复

充值、退费、正常核销和体验核销都是关键资金／次数写入，必须遵守以下规则：

1. 客户端提交前生成稳定 `clientRequestId` 和不包含客户明文、留言、人脸令牌的请求指纹。
2. 请求发出前先把编号和指纹写入客户端持久存储并回读确认。
3. 数据库对幂等键建立唯一约束；同一键同一请求返回原结果，同一键不同请求必须报冲突。
4. 超时、断网、页面关闭或 CloudBase 返回未知结果时，绝不能生成新编号直接重试。
5. 客户端只能携带原 `clientRequestId` 调用恢复查询。
6. 查到完整成功结果后进入原工单详情；查到权威失败且数据库确认不存在时才允许解除锁定。
7. 结果仍不确定时继续锁定并提示查询，不得重复充值、重复扣客户余额或重复扣老师体验次数。
8. 核销的余额／额度扣减、照片绑定、核销记录和设备信号必须采用数据库事务、约束和 outbox 形成一致业务链。
9. 数据库已成功而前端超时是正常可能性，前端提示不得诱导用户再次提交。
10. 所有恢复与重复调用都要有自动化回归测试。

## 20. 数据库迁移和线上版本一致性

- 线上 SQL 与代码版本不一致是 P0 风险。
- 历史迁移必须按编号执行，分段控制台 SQL 必须完整执行并运行只读验证文件。
- 当前关键迁移基线包括 044—062；053 退休老师人脸 Saga，054 改为体验核销验证客户，055 移除老师人脸订单门禁，056 修复体验额度列歧义，057 建立老师创建客户访问关系，058 加强充值和核销完整性，059 固化业务老师选择、归属与真实提交人写权限边界，060 新增与项目完全隔离且禁止删除的简洁产品主档，061 新增随充值原子写入且不可变的产品赠予明细，062 新增总部单独审核的产品购买工单。
- 旧 046／049 文件名或历史 SQL 中仍可能出现老师人脸逻辑；它们是迁移历史，不代表当前规则，不能重新部署旧函数覆盖 053—055。
- 生产部署前必须逐项查询函数、表、触发器、约束和版本是否 READY，不能仅凭 SQL 编辑器显示“执行成功”。
- 云函数 `health` 返回版本必须与源码、README、测试和上传 ZIP 文件名一致。
- 当前仓库基线版本：`staffAccount v75`、`faceRecognition v95`、`verificationPhoto v10`、`teacherCreate v6`。以后修改任一可部署云函数必须按 `AGENTS.md` 同步升级版本。
- 每个发生代码变化的云函数都必须生成 `deployments/<函数名>-v<版本>.zip`，确认 ZIP 根目录、源码、README、测试和文件名版本一致，并在交付时提供可点击的本机绝对路径链接。
- 禁止把未经验证的旧 ZIP 上传覆盖当前函数。
- 任何需要用户在腾讯云执行的 SQL 都必须保存成 `database/cloudbase-console/` 下可整文件复制粘贴的独立 `.sql`；多步操作按编号拆分并由 README 写明顺序。不能只在聊天、Markdown 代码块或 diff 中交付 SQL。

## 21. 安全、隐私与密钥

- 所有人脸、客户照片和核销照片均为敏感数据，默认私有存储。
- SecretId、SecretKey、CloudBase API Key、数据库密码和清理令牌只存在于云函数环境变量或安全密钥管理中。
- 前端、小程序、GitHub、截图、README 和本文件不得包含真实密钥。
- 曾经通过聊天、截图或其他非密钥渠道暴露过的密钥应视为已泄露并轮换，即使当时只是测试环境。
- 小程序只保存公开环境 ID、区域和函数名。
- 微信小程序 AppSecret 的值只能保存在 CloudBase `WX_MICRO_APP` 身份源的服务端密钥配置中；不得放入小程序配置、云函数源码、ZIP、README 或日志。
- 读取照片、客户主页或工单详情必须记录并重新校验当前 UID 权限。
- 账号封存后拒绝新会话和业务写入，但保留历史工单与审计。
- 数据清理采用明确保留清单、事务、执行前预览、外键顺序和执行后只读验证；不得把历史压力测试清理脚本当作日常命令重复执行。

## 22. 响应式 UI 最终要求

- 小程序登录页不显示人物照、联合人物图、“海洋之韵”文字或额外宣传文案。背景使用根据露思卓儿金色水滴／侧脸 Logo 视觉语言单独生成的暖象牙白、浅香槟金竖屏品牌图，只借用清晰可辨的标志轮廓、香槟金色和边缘流动弧线，不嵌入人物与生成文字；精确品牌名始终由 WXML 以“露思卓儿”四字原生渲染。登录表单必须完整位于页面正中的弧形暖白框内，四字标题水平居中，其他标签、输入、按钮和错误提示均不得跑出框；整体颜色不能压得过深，背景中部保持低对比，输入使用弧形细金边，主登录为中等香槟金。“微信手机号登录”原文保留，作为主登录按钮正下方的完整圆角次按钮，文字必须在按钮内横向、纵向严格居中，不再夹在装饰分隔线之间；密码显隐使用固定在密码框垂直中心、有无斜线的眼睛图标，不再显示生硬的“显示／隐藏”文字；登录框内必须保留“修改密码”入口，但入口固定放在密码输入框下方右侧，不能与“登录密码”字段标签挤在同一行。改密页“保存新密码”文字必须在按钮中横向、纵向严格居中；小程序改密必须复用网页版同一套既有手机号短信验证、`shouldCreateUser: false`、60 秒持久化防重发、严格认证响应校验、当前 UID `changeOwnPassword` 和完成后退出临时认证会话规则，不另造账号或业务逻辑。登录后的微信原生导航栏统一显示文字品牌名“露思卓儿”；应用内容区标题以同角色网页版手机端为准，网页版只显示“总部数据看板”等页面标题时，小程序不得再叠加一遍品牌大标题。此前要求所有角色都必须在内容区重复显示品牌名的绝对规则已废止；老师、门店若网页版内容区仍显示品牌结构则继续逐项复刻。
- 老师、门店和总部登录后的首页，在相同手机内容视口下必须与网页版手机端逐项一致，不只是模块结构相似：模块顺序、文案、字号、间距、颜色、卡片边框与圆角、按钮排列、表格列宽和横向滚动方式均按网页版移动布局实现。微信原生状态栏、右上角胶囊、导航栏与浏览器自身地址栏属于平台边界，不计入应用内容区的一致性验收。视觉一致不能破坏客户端隔离：小程序继续使用原生 WXML／WXSS／小程序会话和微信能力，网页版继续使用独立 HTML／CSS／浏览器会话，不互相导入客户端源码。
- 老师手机主页的基础信息只显示“老师姓名、老师短编号”两项，同一横排等宽展示；不再重复显示登录身份、登录手机号、登录账号、“老师本人”标签或身份解释。每个体验项目固定为一条紧凑横排，按“项目名称／编号、当前可用、每月基础、本月已用”利用整行宽度。两处均使用明显可辨的暖象牙白、浅香槟金和深咖色，文字在自己的格内居中并保持单行；不得因桌面版历史网格而在手机端把每个字段或每个项目纵向堆成大卡片。
- 总部进入老师主页后，老师档案、账号管理、体验概览、项目汇总、已配置产品、配置／充值表单与额度历史必须直接沿用其他内部页面同一套暖象牙白 `#fffaf3` 卡片、浅象牙 `#fff8ec` 内层、细金灰边框和深咖重点文字；深咖底色只用于全站共用的顶部栏和少量主操作按钮，低饱和状态色只作小面积标签，不得为老师详情另造一套深色或多色卡片，也不得重新出现大片纯白卡片或旧蓝色主题。
- 总部老师主页不再显示独立、重复的“老师档案”信息卡；老师编号和联系电话与姓名、活跃／封存状态一起放在最上方紧凑档案头中。删除整张重复卡不影响下方保存新临时密码、封存或激活老师的真实操作。
- 总部老师主页的每张“已配置产品”卡片只显示“每月基础／本月已体验”两项等宽指标，内容双向居中并使用放大字号；旧卡片中的“单独充值／最近更新”展示格已废止，不得恢复。该精简只影响产品卡摘要，下方“单独充值体验次数”真实办理表单、额度流水和审计信息必须继续保留。
- 老师额度变更记录较多时必须在记录卡片内部纵向滑动查看全部记录，不再用“查看全部”把整页无限展开；记录数量在卡片标题处显示。
- 桌面端充分利用宽度；办理页可以左右双栏，窄屏自动单栏。
- 手机端的时间范围标题与业务汇总标题保持同一横向导航区域，具体日期可以另起一行。
- 横向滚动只发生在表格、汇总矩阵、时间按钮或明细各自容器内；整页不能左右漂移。
- 时间快捷按钮必须在卡片内等宽排列且不得越界，并沿用暖象牙白、浅香槟金体系；汇总矩阵和数据表先压缩到保持可读性的手机宽度，能完整放进一屏时必须一次显示全部列，只有确实放不下时才在自身容器内横向滚动，禁止用过大的固定宽度强迫滚动。
- 小程序业务明细和客户表的字段、日期、单号均保持单行，空间不足时省略显示而不是拆字换行；业务明细的客户列只显示可点击的客户姓名，客户编号继续作为内部精确跳转参数但不与姓名一起占用表格宽度。
- 小程序业务查询结果的工单号及工单详情页头的完整工单号是上述列表省略规则的明确例外：查询表的单号列必须足够宽并在表格容器内横向滑动；详情标题必须独占整行，二者都要完整显示且绝不换行、截断或使用省略号。详情状态标签放到下一行；通过紧凑字号和可用整行宽度适配窄屏，不能让单号越出自己的单元格或卡片。
- 小程序审核表的表头、内容、状态和操作按钮必须在各自单元格内横向、纵向居中并保持单行；表格总宽度必须与各列宽度之和一致，最后一列不得残留空白、重复边线或多出可滑动的一小截。
- 门店与老师可见的业务查询、客户查询和客户主页历史表同样执行精确列宽规则：表格宽度等于可见列宽之和，表头与内容双向居中并保持单行；能缩短的列先缩短，只有字段数量确实超出手机宽度时才在表格容器内横向滑动。客户、拍照和确认组件沿用暖象牙白、浅香槟金、深咖及低饱和语义色，禁止在内部页面混回旧版高饱和蓝紫色。
- 小程序客户查询结果是只读入口，列顺序固定为“姓名、充值、核销、门店、生日、业务阶段、建档日期、客户状态”，客户状态必须是最后一列；查询页不再放封存／恢复按钮，姓名整行进入客户主页后才可由总部或门店执行状态操作。客户主页的项目汇总和业务记录只显示项目／产品名称，业务老师只显示姓名，产品编号和老师编号仅作为内部数据保留。业务记录必须拆成“充值、退费、核销、体验、产品”五个独立标签；“产品”只读取独立产品购买工单，数量以件显示；充值与退费由服务端分别按 `NEW`／`REFUND` 筛选，五类都由服务端各自维护稳定游标分页，不能只在客户端把同一批记录改个标签。各类记录按实际字段和状态长度使用紧凑自适应列宽，不能用统一超宽状态列制造无意义间距。
- 小程序客户主页的客户备注、客户留言和留言输入区统一使用暖象牙白、浅香槟金及深咖配色；备注展示框、编辑框、留言列表和输入卡必须留在父卡片内部，左右留白严格对称，带内边距的 `scroll-view` 不得因宽度计算越出右侧边界。
- 滑动时标题、固定列和背景裁剪必须正确，不能出现标题残留在页面左侧。
- 表格行高、按钮高、输入框高和并列卡片高度保持一致。
- 操作按钮使用统一语义色：主操作、普通操作、成功、警告、封存／危险各自一致。
- 手机摄像头、相册保存、照片重读和表格分页必须使用真实设备回归，不以桌面浏览器模拟器代替全部测试。核销详情中数据库声明存在但缩略图暂不可用的照片位，整个占位框和框内按钮都能触发该位置的单张重读；单张请求期间只锁定该照片位，失败信息也只显示在该照片位。
- 小程序在现有页面范围内与网页版手机端采用同一视觉层级、颜色、紧凑卡片、表单顺序、两列快捷入口和容器内横向数据表；只复用布局规则，不复制或导入网页 HTML、CSS、JavaScript。
- 微信原生导航、手机号授权、相机、相册和安全区继续使用小程序平台能力，不为像素一致而跨端共享客户端代码。
- 小程序登录后内部页面统一使用登录页延伸出的暖象牙白、浅香槟金、深咖与低饱和绿／紫／陶色语义配色；不得恢复旧版高饱和蓝色工作台主题。一般业务页面的这项配色仍只修改小程序 WXML／WXSS，不反向覆盖网页版客户端样式；充值／退费／核销凭证及产品模板四种成品预览是明确例外，网页和小程序两端都采用同一套暖象牙白、浅香槟金、深咖凭证视觉规范，但各自在自己的客户端目录保存背景资产和实现渲染，禁止跨目录运行时引用。
- 总部首页的移动结构和逻辑以网页版总部手机端为唯一基准：顶部入口依次提供“总／查／管／审／退”，总部内容区不重复品牌大标题。总部查询的默认组合固定为“今天＋门店＋全部项目＋充值”；时间周期、排名对象、项目范围和排序指标必须放在同一个筛选卡中，并按这个顺序排列，重置筛选也必须回到同一默认组合。日期筛选默认只显示周期，自定义周期才展开开始／结束日期。筛选卡后直接显示“项目汇总”，按项目名称稳定排列并逐行显示核销、充值、体验、退费；必须统计 `public.products` 的全部项目，项目过多时服务端分页，每页 10 项，底部合计始终是当前日期范围内全部项目的全局合计而非当前页小计。项目范围选择器同样保留有效和已封存项目，已封存项必须明确标注“已封存”，以便总部查询其历史数据。总部首页不再单列四项业务总数及门店／老师数量六张指标卡，也不再显示重复的“分类统计／前 10 名”卡；四类总数只保留在项目汇总合计行，排名统一由下方完整排名呈现。排名由三个独立选择共同确定：排名对象只允许“门店／老师”，项目范围允许“全部项目／某个具体项目”，排序指标只允许“充值／核销／体验／退费”；服务端必须按所选项目过滤后，再按所选业务次数降序，不能在客户端对当前页假排序。每条结果同时保留四类次数。移动端完整排名使用不越界的卡片，不恢复旧版八列宽表；长名称单行省略，所选业务次数和占比必须保留安全边距。上一页／下一页、页码跳转、单独重试和完整矢量 PDF 导出均必须真实可用；PDF 必须包含当前筛选、全部项目汇总及完整排名，表格文字和线条均使用 PDF 字体／路径对象，不得把页面或表格栅格化为 JPEG、PNG 或 Canvas 截图；多页自动分页。排名翻页和换选择只重读排名，不得无故重载项目汇总。旧版六张总部指标卡、三张纵向图、单独前 10 名分类卡、把项目当成排名对象、按四类合计排序、CSV 导出及八列横向排名表均已明确退役。
- 总部首页筛选卡底部不再重复显示日期、全部门店、全部项目、全部老师、全部客户或数据库更新时间；这些信息已经由上方筛选控件和数据库统计范围详情表达。筛选卡底部只保留单行、紧凑且双向居中的“重置筛选”按钮。
- 手机端顶部“办／查／管／审”均采用与网页版相同的栏下单列下拉框，不得改成两列快捷卡片或页面中部弹窗；同一时间只展开一项，点击下拉框外空白区域立即收起。
- 手机端分页按钮使用紧凑字号与高度，按钮文字必须横向、纵向居中；“上一页／页码／下一页”保持同一行，总部排名的页码跳转放在紧邻的第二行，禁止把上一页和下一页拆到上下两端。
- 小程序充值、退费、正常核销与体验核销在服务端返回完整工单 ID、工单编号和最终状态后，必须立即跳转到对应工单详情；网络不确定结果先按原防重编号恢复，恢复到完整原单据后同样跳转。内部防重状态与网页版一致：成功响应只把原请求标记为 `CONFIRMED` 并继续保留锁，详情页按同一请求编号成功回读同一工单后才清锁；跳转失败或详情不匹配不得提前清锁。不得只在原表单底部显示成功提示；详情跳转失败时必须保留工单编号并指引从查询页进入，且禁止因此重复提交。
- 小程序与网页版的充值页均以“第一步确认客户、第二步填写充值信息、第三步填写产品赠予”呈现；老师的发生门店选择是进入三步业务前的范围选择，不改变这三个步骤的编号。第三步先选激活产品和数量，再点圆形“＋”加入；已加入行必须显示产品名称、编号、数量和删除操作，空清单仍可正常提交充值。

## 23. 小程序与 BLE

当前原生小程序仓库覆盖的目标范围：

- 手机号＋密码登录、用户点击授权的微信手机号快捷登录和服务端角色回读。
- 老师／门店工作台。
- 总部／门店客户查询：总部可查看全部或指定门店，门店锁定本店；支持业务阶段、活跃／封存、建档日期、姓名／生日、汇总、分页和直接跳页。查询结果保持只读，客户状态修改只放在客户主页并继续要求二次确认与数据库回读。老师不进入这张通用查询页，只从老师首页的本人业务明细和有权限的客户列表进入客户主页；旧的“老师客户页保持只读”入口描述已废止。
- 总部／门店充值与核销查询：支持门店范围、项目、业务类型、审核状态、日期、姓名／生日、汇总、分页和直接跳页；历史补录／作废数据保留只读审计筛选。
- 客户主页、客户建立和精确工单详情；首页业务明细、查询结果和客户历史中的工单号均进入同一详情页，核销详情按当前 UID 重新鉴权读取缩略图和高清原图。客户主页照片只自动读取、展示并允许点击预览，不再显示“重读原图”或“保存到相册”；该精简不影响核销工单照片。正常核销和体验核销写入即完成，不存在审核流程，查询与工单详情不得显示“审核时间”；只有历史补录核销的审核记录可以显示审核状态与审核时间。核销工单必须复用网页版单据模板语义提供 PDF 与图片导出；五个照片位逐张提供高清原图预览和直接保存到微信系统相册，并对相册权限拒绝、临时地址失效和下载失败给出可重试提示。仍提供相册保存的工单照片或单据图片在相册权限被拒绝后，每次再次点击保存都必须重新询问是否打开设置；用户开启权限后自动继续本次保存，不得要求重新点一遍，也不得把一次拒绝记成永久静默失败。
- 网页与小程序的充值／退费／核销凭证及产品模板四种样例预览统一使用露思卓儿金色水滴／侧脸视觉语言的独立暖象牙白品牌背景；中心必须保持低对比大留白，浅香槟金只作边缘和水印，正文、表格与标题使用深咖和低饱和灰棕，不能恢复旧蓝灰单据主题。背景图片不得烘焙品牌文字、工单字段、人物、海洋之蕴或任何产品名称，所有真实文字与产品 LOGO 继续由程序动态绘制。网页与小程序分别保存同源成品图，产品预览、真实长图和 PDF 每页都必须通过各端共用凭证渲染器显示该背景；背景载入失败时不得悄悄导出无背景文件。
- 充值／退费凭证顶部资料固定为“客户姓名＋完整客户编号／项目”第一行、“门店／业务老师”第二行；充值信息只显示次数、提交时间和审核时间，不显示业务前余额或业务后余额。只有工单确有产品赠予时才另起“赠予产品”板块，逐项只打印产品名称和数量，不打印产品编号；没有赠品时整个板块不得出现。
- 小程序充值／退费／核销凭证的“客户”格固定独占一整行，按“客户姓名 · 客户编号”完整打印；产品样例用同样格式显示样例编号。该格使用整行可用宽度自适应字号并限制绘制边界，绝不换行，也不得画出卡片；客户编号不能为了适配而删除或用省略号替代。
- 小程序工单和产品样例导出的 PDF 使用标准 A4 页面；内容超过一页时按 A4 真正分页，不能通过生成异常长的单页假装完整。当前小程序端采用逐页 `2480 × 3508` 像素（300 DPI）高质量渲染后写入 A4 PDF，图片导出同样保留高分辨率；这是高清位图交付，不能在文案中冒充矢量文件。图片格式本身不可能无限无损放大；如果以后要求动态中文文字、线框和品牌曲线都为真正矢量，必须另建带嵌入中文字体、服务端鉴权和私有文件交付的独立导出服务，并按云函数版本／README／ZIP 规则交付。产品模板的四种样例预览和真实工单导出必须共享同一单据排版语义，不能分别维护两套看起来不同的凭证。
- 充值、退费、正常核销和老师体验核销。
- 客户 1:1 人脸验证与原图保存。
- P0 防重和原请求恢复。
- 总部门店、老师目录复刻网页版手机端各自的真实工作流：门店按名称／联系人电话查询，老师按姓名／联系电话查询；查询结果、活跃和封存数据使用容器内横向表格，新增进入独立创建页，名称本身作为进入独立主页的唯一入口，不再重复设置“查看／进入主页”操作列，也不再用通用弹窗拼凑详情。老师目录只显示姓名、编号、联系电话和状态四列，四列必须自适应占满卡片可用宽度并保持左右留白一致，姓名直接进入老师主页；“体验额度”“账号操作”“配置／充值”目录列已废止，体验配置、充值和状态管理统一放在老师主页。门店主页必须包含基础资料、时间范围、项目汇总、四类有效业务明细、工单／客户精确跳转、服务端分页、活跃／封存客户和总部状态操作；老师主页必须包含老师档案、密码重置、账号状态、体验总览、项目累计、已配置额度、配置新产品、单独充值、删除配置和额度历史。创建和所有写操作必须由同一 CloudBase 云函数与 PostgreSQL 规则确认；状态修改后必须重新读取数据库，禁止只在小程序本地改显示。
- 小程序总部首页保留“总／查／管／审／退”主导航；进入产品、门店或老师管理及其创建／详情页后不得再次显示这条主导航，管理内页统一使用微信原生返回键及页面内明确的返回按钮，避免重复占据顶部空间。
- 总部项目管理必须复刻网页版手机端的专用流程，不能套用门店／老师通用查询目录：列表页只显示“全部项目”和“点击项目进入单据模板设置”，不显示项目查询、重置或活跃／封存分区；新增项目进入独立创建页，创建成功直接进入该项目模板页；项目模板页提供项目封存／激活、共用原始 LOGO、正常核销与体验核销共用说明、充值与退费共用说明、保存后数据库独立回读核对，以及核销 PDF、核销图片、充值 PDF、充值图片四种样例预览和导出。既有客户端路由名和服务端 `product*` 接口名为兼容保留，不得据此把用户文案改回“产品”。
- 小程序项目模板页的 LOGO 区只显示图片、“共用项目 LOGO”和选择／上传／移除操作；不显示对象编号、文件名、文件大小、像素尺寸或“不压缩／不裁切”等实现说明。服务端仍必须保存和回读校验原始图片，不得因前端精简文案而降低完整性检查。
- 总部另设与项目管理并列的“产品管理”，用于涂抹类／实物产品。产品管理必须沿用项目管理的“列表页 → 独立创建页”操作结构：列表页不内嵌名称输入框，只提供“新增产品”入口；点击后进入独立创建页，并且该页只允许填写一个产品名称，成功后返回列表并重新读取数据库。`public.retail_products` 只保存数据库自动生成的 `PDT...` 产品编号、产品名称、活跃／封存状态和内部审计字段；不提供类别、介绍、LOGO、模板、次数或业务配置。产品只能封存和重新激活，客户端与云函数都不得提供删除动作，数据库删除保护触发器必须拒绝物理删除；同名产品封存后应激活原记录而不是重复新增。产品不得进入 `public.products` 项目主档、充值／退费／核销／体验下拉框、客户余额、项目统计、老师体验额度或凭证模板。旧版在产品列表页直接填写名称并提交的内嵌创建表单已经退役。
- 总部充值、退费和核销审核：总部首页“审”入口保持网页版的充值／核销两项，充值审核页内继续提供网页版已有的充值／退费切换；三类均支持门店、状态、工单编号查询，100 条分页、直接跳页、工单／客户详情，以及带可选留言的通过／驳回。审核写操作继续由 `staffAccount.reviewOrder` 重新校验总部身份。

小程序规则：

- 密码登录和微信手机号授权必须进入同一既有 CloudBase UID；微信授权不是新的业务身份。
- 快捷登录只接收微信按钮返回的一次性授权 code，并交给 CloudBase SDK 验证；小程序不解密、不记录、不持久化手机号授权 code。
- `session` 不接收手机号作为绑定依据，只按当前 CloudBase UID 回读角色、账号状态和门店范围。
- 总部可登录查看，但不显示一线办理入口。
- 门店没有体验核销。
- 门店充值／退费的业务老师可选，正常核销的业务老师必选；所选老师必须由服务端验证为活跃老师。
- 老师账号四类业务都自动绑定当前老师本人，业务老师必填且不可改选。
- 老师主页只提供建档、充值／退费、正常核销和体验核销的业务入口，不在主页提前选择或缓存办理门店。老师进入每一个具体业务页后，第一步都必须重新读取并选择真实活跃门店；未在当前页面选定门店时不能读取客户或提交业务。切换门店必须同时清除上一个门店的客户、老师、产品、余额、人脸证据和未提交表单状态。门店账号不显示该选择器，继续由服务端 UID 锁定绑定门店。
- 老师选择门店后，充值、退费、正常核销和体验核销的“现有客户”必须与网页版一样使用原生下拉选择器，办理页不得直接铺开全部客户按钮或固定高度客户列表。下拉框启用前由客户端按服务端稳定游标自动读完该门店全部活跃客户，每批最多 100 位；下拉框内部可上下滑动选择全部客户，不能永久停在前 100 位，也不能把第 101 位以后的客户强制改成只能通过姓名＋生日查询。门店账号同样使用本店全量客户下拉框；姓名＋生日保留为并行精确查找方式。切换门店、切换查找方式或页面卸载必须使旧门店的分页响应失效，禁止把旧客户混入新门店下拉框。旧版在办理页直接展开客户按钮并靠固定区域滚动加载的设计已经退役。
- 正常和体验核销都验证客户，不验证老师。
- 小程序只调用现有云函数，不在本地复制 SQL 余额算法。
- 小程序查询入口与网页版保持同一角色范围和筛选语义，但 WXML／WXSS、路由与会话继续物理隔离；任何筛选参数都不能扩大服务端按 UID 计算的门店／老师权限。
- 所有可快速切换筛选、门店、客户、产品、余额或页码的异步读取都必须使用不可变请求快照和递增请求代次；筛选变化、页面卸载或新请求发出后，旧响应不得覆盖新页面状态。失败响应必须清空与本次请求不一致的行、汇总、页码和游标，禁止残留上一条件的数据。
- 小程序启动时本地会话只能作为待服务端复核的缓存，不能直接作为进入工作台的权限依据。`restoreAndValidateSession` 完成前，登录、微信手机号登录、修改密码和任何深链页面均须 fail-closed；完成后客户端只信本次服务端回读写入的 `globalData.session`，不得回退到可能失效的本地缓存。
- 小程序工单详情只有在服务端回读的数据库 ID、完整单号、业务大类和细分类都与路由快照完全一致时才可渲染，也只有同一工单与原 `clientRequestId` 一致时才可解除提交防重锁。错单、错码、错类型或回读不完整时必须保持锁定。
- 总部给老师充值体验额度必须在本地持久化老师、产品、次数、说明、原请求号与指纹；断网、超时或重启后只能重放同一请求。只有服务端返回同一充值流水的精确证明，并重新读取同一老师、同一产品成功后才能解除锁定。

CloudBase 和微信发布前置：

- 在与现有 Auth 用户、云函数和 PostgreSQL 相同的 CloudBase 环境中启用 `WX_MICRO_APP` 身份源，配置当前小程序 AppID 和 AppSecret。
- 身份源必须显式配置 `On=TRUE`、`AutoSignInWhenPhoneNumberMatch=TRUE`、`AutoSignUpWithProviderUser=FALSE`、`TransparentMode=FALSE`、`ReuseUserId=FALSE`；手机号＋密码登录继续保留，不得依赖控制台默认值。
- 小程序主体必须是已完成微信认证的非个人主体；发布前完成手机号用途的用户隐私保护指引、对应接口声明和明确授权文案。
- 非个人主体小程序未通过微信认证、未开通云开发或未完成 CloudBase 小程序授权时，密码与微信手机号两种小程序登录均不得宣称验收通过。SDK 返回空白 `UNKNOWN` 只能说明客户端没有取得可用错误详情，不能据此断定账号密码错误或认证状态；客户端应提示检查网络和 `request` 合法域名，并保留服务端返回的明确认证错误。
- 按微信和 CloudBase 当期规则开通小程序手机号验证能力、套餐或计费，并在真机完成授权成功、拒绝授权、未绑定员工和封存员工回归；开发者工具模拟不代表发布验收。

BLE 后续实现顺序：

1. 服务端首先确认核销事务成功并生成唯一设备信号。
2. 小程序取得一次性、可审计、不可重复消费的开机授权。
3. 小程序使用 BLE 搜索指定设备、连接、写入指令并回报结果。
4. BLE 失败只影响设备执行状态，不得回滚或重复扣减业务次数。
5. 重试 BLE 必须复用原设备信号，不重新创建核销工单。

## 24. 明确废弃、禁止恢复的旧设计

以下规则已经被后续决定明确推翻：

- 废弃运营角色、运营管理、运营账号和运营门店范围。
- 废弃老师创建时强制照片或人脸。
- 废弃老师补录／替换人脸页面。
- 废弃“老师没有人脸不能激活、充值、退费或核销”的规则。
- 废弃体验核销扫描老师人脸；新体验核销只扫描客户。
- 废弃门店体验核销入口与权限；体验只允许老师账号。
- 废弃总部办理客户建立、充值、退费和核销。
- 废弃 1:N 人脸搜索；始终是已选客户的 1:1。
- 废弃补录核销入口；只保留正常核销和老师体验核销。
- 废弃核销普通撤销／作废入口；保留不可变凭证和受控补偿。
- 废弃“只有老师账号亲自提交才算业务老师”的旧口径；新口径按服务端验证并写入工单的 `teacher_id` 归属统计与客户关系，门店可把业务归到所选老师，老师提交必须归本人；历史总部、退役角色或老师身份不匹配的旧字段不可信。所有写权限仍只认真实 `submitted_by_account_id`。
- 废弃无限滚动大表和一次性返回全部总部汇总数据。
- 废弃在导出时用空占位替代数据库中真实存在但读取失败的照片。
- 废弃为了兼容旧老师人脸 Saga 而保留复杂创建流程。

## 25. 自动化与人工验收最低标准

每次涉及业务、权限、数据库、云函数或照片的修改至少完成：

- JavaScript 语法检查。
- 仓库全部 `node --test tests/*.test.js`。
- 云函数源码版本、README、ZIP 根目录 README 和 ZIP 文件名一致性测试。
- 敏感信息扫描。
- 数据库迁移只读验证。
- 总部、门店、老师三个真实角色的路由和服务端权限测试。
- 充值和退费：成功、拒绝、重复点击、超时恢复、不同请求复用同一幂等键。
- 正常核销：客户余额足够／不足、照片失败、人脸失败、事务成功但前端超时、设备信号重复消费。
- 体验核销：老师额度足够／不足、门店／总部绕过调用拒绝、只扫客户、客户余额不变、额度只扣一次。
- 客户跨老师读取：有关系老师可读全部历史，无关系老师拒绝，其他提交人写操作拒绝。
- 五张照片：首屏缩略图、单张原图重读、滑动后重载、补充照片截止、原图下载、PDF／图片缺图时阻止导出。
- 手机端：横向单行记录、容器内滚动、跳页、相机、保存相册和返回导航。
- 登录：密码与微信授权返回同一 UID；未绑定手机号不生成 Auth 或业务账号；客户端伪造手机号不能改绑 UID；封存账号和用户拒绝授权均失败关闭。
- 业务老师矩阵：门店充值／退费可选老师、正常核销必选老师、门店体验拒绝；老师提交自动绑定本人；选中老师获得统计与只读客户关系但不能修改或补照片到非本人提交工单。

## 26. 当前数据与环境操作原则

- 代码仓库是公开仓库；不得把生产账号名单、手机号、客户姓名、人脸照片、云函数密钥或数据库导出提交到 GitHub。
- 压力测试数据清理是一次性受控运维，不是应用启动逻辑。
- 任何再次清库必须由用户明确确认当前保留清单，并先输出只读预览；不能依据旧聊天中的历史名单自动执行。
- 代码推送不代表云函数、SQL 或静态托管已经部署。交接时必须分别说明：GitHub 已推送、SQL 是否已执行、云函数是否已上传、静态网站是否已发布、health 是否已验证。
- 代码和文档完成且验证通过后必须直接提交并推送当前分支到 `origin`，再核对本地 HEAD 与上游 HEAD 一致；不得 force-push。推送失败时不能宣称交付完成。
- 本轮交付边界：CloudBase 环境已为开发 AppID 完成小程序授权；小程序使用独立 CloudBase JS SDK 连接原有 PostgreSQL 环境，不执行开发者工具的云环境转换，也不新建第二套数据库。手机号＋密码已在开发者工具中通过真实既有账号进入总部主页并完成 `staffAccount.session` 回读。小程序 `0.2.19` 已于 2026-08-26 上传为开发版本，总包 `1,762,807` 字节、主包 `1,502,974` 字节；管理员仍需在微信公众平台“版本管理”中将其设为体验版。网页版 `0.15.14` 同日安全发布，本轮 10 个变更文件完成远端一致性校验。这不等于提交小程序审核或正式发布。当前仓库前端已经增加门店／老师独立产品购买、总部独立购买审核、客户“产品名称／购买／赠送”三列汇总，以及客户底部业务记录中的第五项“产品”；这些新页面和改动尚未因此自动发布。迁移 060、061、062 仍需由用户在 CloudBase 控制台依次执行并只读验收，`staffAccount-v75.zip` 与 `faceRecognition-v95.zip` 仍需人工上传，线上 `staffAccount` 上次确认仍为 `v69`，因此独立产品、充值赠品和产品购买目前都不能视为线上可用；未添加赠品的既有充值继续兼容。`WX_MICRO_APP` 身份源、微信手机号能力与计费及正式 `request` 合法域名仍需平台侧完成真机端到端确认；CloudBase 服务商域名入口本月修改额度已经用完，禁止继续重复提交。代码推送、SQL、云函数、网页发布、小程序开发版上传、设为体验版、提交审核和正式发布状态仍分别计算，不得因本地登录或开发版上传成功而宣称线上快捷登录已正式发布。

## 27. 另一台电脑接手步骤

```bash
git clone https://github.com/Zhixiang010607/lusizhuoer.git
cd lusizhuoer
git checkout main
git pull origin main
```

如需开发原生微信小程序，使用 Node.js `>=20.19.0`（当前已验证 `24.19.0` LTS）和仓库声明的 pnpm `9.15.9`，然后从仓库根目录执行：

```bash
corepack enable pnpm
corepack install --global pnpm@9.15.9
cd miniprogram-app/miniprogram
pnpm install --frozen-lockfile
```

`miniprogram/.npmrc` 已固定 `node-linker=hoisted`，避免微信开发者工具遇到 pnpm 默认符号链接布局。微信开发者工具应导入 `miniprogram-app`，再执行“工具 → 构建 npm”。现有环境是 CloudBase PostgreSQL 环境，开发者工具提示“不支持 PostgreSQL 环境”时不要执行“转换云环境”；小程序通过仓库内 CloudBase JS SDK 与微信适配器直连同一环境并调用现有云函数。当前开发 AppID 为 `wxb053c1bd6c684d8b`；更换 AppID 时必须同步确认微信公众平台合法域名和 CloudBase 环境授权，不能只改本地 JSON。AppID 不是密钥，但 SecretId、SecretKey、CloudBase API Key、数据库密码和真实业务数据仍严禁进入仓库。

然后按以下顺序读取：

1. `PROJECT_CONTEXT.md`（本文件，最终业务规则）。
2. `AGENTS.md`（云函数版本发布纪律）。
3. 根目录 `README.md`（页面、迁移和部署细节）。
4. `miniprogram-app/README.md`（小程序导入和验收）。
5. 正在修改的云函数目录 README 和相关测试。

给新 Codex 任务的推荐首条指令：

> 请先完整阅读 `PROJECT_CONTEXT.md` 和 `AGENTS.md`，再检查当前代码与它们是否一致。`PROJECT_CONTEXT.md` 记录最终业务规则；不要恢复其中“明确废弃”的旧老师人脸、运营角色、门店体验或总部办理逻辑。修改后运行全部测试，并同步更新规则、版本和 README。

## 28. 交付完成定义

一次修改只有同时满足以下条件才算完成：

- 最终业务规则明确，没有新旧规则并存。
- 服务端权限和数据库约束已经实现，不能只隐藏按钮。
- 网页和小程序使用同一业务语义。
- 幂等、超时恢复、余额／额度、照片和设备信号通过对应回归。
- README、`PROJECT_CONTEXT.md`、云函数版本和部署包同步。
- 需要用户执行的 SQL 已提供可整文件复制的独立 `.sql` 链接，并明确顺序与是否已执行。
- 每个有代码变化的云函数均已提供同版本、ZIP 根目录正确且可点击下载的部署包链接。
- 没有敏感信息进入提交。
- 验证完成后已提交并推送，GitHub 上游提交与本地 HEAD 一致；推送失败时交付仍未完成。
- 如果尚未部署，交付说明明确写出“只推送代码，尚未部署”，不得让接手者误以为线上已经更新。
