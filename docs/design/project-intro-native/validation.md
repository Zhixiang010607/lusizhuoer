# 原生预览验证 · 2026-09-07

## 自动化

`node --test tests/*.test.js`：**322 项通过，0 失败**。

覆盖既有三类账号与业务契约；新增公开介绍白名单、三个项目内容／本地资产、滚动章节往返、动效关闭、短屏普通流、过期测量回调、返回／替换路由、失效入口、图像失败兜底、须知折叠及离页后延迟渲染不得滚动新页。登录页新增隐藏生命周期失效检查，验证阅读介绍不会被延迟完成的启动会话强行送回首页，返回登录仍使用原会话验证结果。

官方微信开发者工具 CLI `build-npm` 成功。保留既有 `bson.cjs.js` 依赖入口告警，不将它描述成零告警构建。原始主包加可达 SDK 的估算为 **1,881,505 字节**，低于仓库 1.8 MiB 阈值与微信 2 MiB 限制；新增介绍内容和完整主视觉位于独立分包。

## 微信开发者工具

实际项目路径为仓库的 `miniprogram-app`。没有用浏览器 HTML、截图拟态或业务鉴权绕过代替原生页面。测试期间没有填写真实密码、申请手机号授权或写入业务数据。

已完成三种实际模拟器尺寸的完整公开页面检查：

| 模拟器 | 工具报告尺寸 | 结果 |
| --- | --- | --- |
| iPhone 15 Pro Max | 430 × 932 | 三项目入口、图像、章节、关闭动效、须知、互跳与返回通过 |
| iPhone 5 | 320 × 568 | 三项目普通文档流、边界、章节、须知与返回通过 |
| iPad | 768 × 1024 | 三项目固定字号、宽度约束、章节、须知与返回通过 |

对每个项目依次切换章节 `0 → 1 → 2 → 1 → 0`，然后关闭动效并再次切换；返回及项目互跳不累积页面栈。须知默认收起，展开与收起均验证。无效项目键可返回登录。标题、主视觉、工具栏、正文、图解、章节按钮和页脚均未超出报告视口宽度。

自动化脚本在热重载／路由动画期间曾读到旧页句柄而报 `page is not on top of page stack`，增加对真实目标路由就绪的等待后完成上述检查；这不是通过放宽业务鉴权或修改生产会话规避的结果。截图和边界记录来自最终原生内容。最后两项生命周期防竞态调整另有自动化回归覆盖。

截图位于本目录 `screenshots/`。这些是**微信开发者工具模拟器截图**，不是物理手机或 iPad 真机验收。

## 扫码与发布状态

此前官方 CLI `preview` 连续两次返回 **`code 10 / Timeout`**，没有二维码和成功回执。2026-09-07 继续排查时，界面预览也停在上传阶段；基础库另报 `40001 / invalid credential`。结束卡住的任务、由用户重新扫码登录并重启开发者工具后，官方 CLI 于悉尼时间 **14:00** 返回 **`✔ preview`**，实际生成新的预览二维码与包体回执。不能仅凭恢复顺序断言网络、代码或凭证是唯一根因。

本次成功预览对应源码 `efb340b6f4114ef66d8e15c3b23b0a13d778dab1`。官方包体回执保存为 [preview-package-info.json](preview-package-info.json)：主包 **1,819,633 字节**，项目介绍分包 **211,719 字节**，总包 **4,807,552 字节**；各独立包均低于 2 MiB。二维码已检查为有效的 470 × 470 JPEG 并交付用户，按实际格式保存至忽略目录 `output/project-intro-native/preview-qr.jpg`，不把登录码或预览二维码提交到公开仓库。

这证明本次扫码预览包已生成，不代表物理手机实际扫码和交互验收已完成。最后确认上传成功的开发版本仍是 `0.2.70`；本轮没有新的开发版本上传、体验版关联回读、审核或正式发布。旧浏览器提案仅作为设计历史，不能代替原生手机验收。

本次恢复没有修改客户端或服务端代码，没有新 SQL、云函数、数据库或静态 Web 部署。用户可用新二维码继续真机验收。
